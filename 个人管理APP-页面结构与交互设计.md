# 个人管理 App · 页面结构与交互设计

> 状态：**待确认**　版本：v1.1　日期：2026-09-20
> 前置文档：`个人管理APP-方案-v1.md`（字段定义以那份为准，本文只讲结构和交互）

---

## 0. 上一版三个待确认点，先定下来

| 问题 | 决定 |
| --- | --- |
| 字段有没有多 / 少 | 保持方案 v1，不增不减 |
| 工时计时器、健身热力图 | 第一版**保留**，不砍 |
| 备份方式 | 手动一键导出；距上次备份超过 7 天时，顶部出现一条浅色提示条（不弹窗、可手动关掉） |

---

## 1. 应用外壳

整个 App 是一个单页，十个页面共用一层外壳，切换靠 hash 路由，不刷新浏览器。

```
App
├── Sidebar 侧边栏（固定，宽 200px）
│   ├── 顶部：应用名
│   ├── 导航组
│   │   ├── 首页总览 / 今日
│   │   ├── 学习计划 / 开发工作 / 咨询工作
│   │   ├── 健身计划 / 饮食计划 / 游戏娱乐
│   │   └── 数据与设置
│   └── 底部：数据条数摘要 + 主题切换按钮
│
├── Main 主区
│   ├── Topbar 顶栏
│   │   ├── 左：当前页面标题 + 一句话说明
│   │   ├── 中：全局搜索框（搜任务 / 笔记 / 游戏 / 客户 / 项目）
│   │   └── 右：快捷新增按钮 + 主题切换
│   └── PageHost 内容区（按路由渲染当前页面）
│
└── Overlay 全局浮层
    ├── Drawer 右侧抽屉 —— 复杂表单（新增长表单、任务详情、客户详情）
    ├── Confirm 二次确认框 —— 删除、清空等危险操作
    └── Toast 右下角提示 —— 操作反馈，3 秒自动消失
```

**路由表**

| 路由 | 页面 |
| --- | --- |
| `#/home` | 首页总览 |
| `#/today` | 今日 |
| `#/study` | 学习计划 |
| `#/dev` | 开发工作 |
| `#/dev/project/:id` | 项目详情（看板） |
| `#/consult` | 咨询工作 |
| `#/consult/client/:id` | 客户详情 |
| `#/fitness` | 健身计划 |
| `#/diet` | 饮食计划 |
| `#/game` | 游戏娱乐 |
| `#/settings` | 数据与设置 |

---

## 2. 全局交互规则

十条，全站统一，不搞花样。

| # | 规则 | 说明 |
| --- | --- | --- |
| 1 | 新增 | 简单条目用行内输入框，敲回车即建；超过 3 个字段的表单走右侧抽屉 |
| 2 | 编辑 | 点击文字就地变输入框，失焦自动存；按 Esc 取消 |
| 3 | 删除 | 一律先弹确认；删完 Toast 里带「撤销」，5 秒内可恢复 |
| 4 | 拖拽 | 今日计划排序、开发看板换状态、周计划排序三处使用 |
| 5 | 保存 | 没有「保存」按钮，任何改动即时落库 |
| 6 | 键盘 | `/` 聚焦搜索、`N` 新增、`Ctrl + 1~9` 切页面、`Esc` 关浮层 |
| 7 | 空状态 | 每个列表都有引导文案 + 一个主操作按钮，不留空白页 |
| 8 | 筛选 | 列表页统一在顶部有一行筛选条：状态 / 日期范围 / 关键词 |
| 9 | 计算 | 首页摘要每次切页重算，不需要手动刷新 |
| 10 | 破坏性操作 | 清空数据需要输入「确认清空」四个字才能点确认 |

---

## 3. 通用组件清单（跨模块复用）

卡片 `Card`、列表行 `Row`、看板列 `Board`、标签 `Tag`、进度条 `Progress`、勾选框 `Check`、条形图 `Bar`、热力图 `Heatmap`、抽屉 `Drawer`、确认框 `Confirm`、提示 `Toast`、空状态 `Empty`、筛选条 `FilterBar`、日期切换 `DateNav`、页签 `Tabs`。

只做这套，页面里不各自造轮子。

---

## 4. 逐页结构 + 交互

### 4.1 首页总览 `#/home`

三栏栅格，宽屏三列，窄屏两列、单列。

```
首页总览
├── 行1  ├── 今日计划卡（约 5 条未完成，直接可勾）
│        ├── 今日打卡卡（健身 / 饮食状态，可补打）
│        └── 学习进度卡（进行中的学习项 + 进度条，最多 3 条）
├── 行2  ├── 项目进展卡（开发 / 咨询进行中项目 + 任务完成比）
│        └── 本周工时卡（累计工时，按项目分条）
└── 行3  ├── 游戏时长卡（本周累计 + 最近在玩）
         └── 快速备忘卡（最近便签，就地编辑）
```

交互点：

- 点卡片标题区 → 跳对应模块；勾选框、输入框这类控件就地响应，不跳页
- 卡片悬停时右上角浮出「…」，菜单里是「进入模块 / 隐藏此卡」
- 快速备忘卡底部有一个输入框，可直接新增便签
- 所有摘要都是实时算的，今天干了什么，首页立刻就变

### 4.2 今日 `#/today`

这是每天停留最久的页面，左中右三栏，比例 5 : 3.5 : 3。

```
今日
├── 左栏 · 今日计划
│   ├── 输入区：文本框 + 优先级选择（默认中）+ 关联模块下拉
│   ├── 任务列表（按优先级排，同级按添加时间）
│   │   └── 行：勾选框 | 标题 | 优先级色点 | 来源标签 | 悬浮操作
│   └── 底部：已完成 3 / 8　+「隐藏已完成」开关
│
├── 中栏 · 快速备忘
│   ├── 便签列表（点击就地编辑，支持置顶 / 删除）
│   └── 底部「＋ 新增便签」
│
└── 右栏 · 打卡 + 记录
    ├── 健身打卡：一个大按钮，点一下=今天练了，再点取消；旁边显示连续天数
    ├── 饮食打卡：早 / 午 / 晚 / 加餐 四行，每行勾选框 + 内容快捷填
    └── 今日记录：今天产生的学习 / 工时 / 游戏记录，各带「＋ 记一笔」
```

交互点：

- 任务行悬浮出现四个操作：就地编辑、顺延到明天、移到其他模块、删除
- 任务可拖动排序；拖到左栏顶部即置顶
- 从别处「加进今日计划」的条目，会自动带上来源标签（如「来自：XX 客户纪要」）
- 勾选完成时，标题变浅并带删除线，不消失，方便回看
- 日期切换：右上角可切到昨天补记，标题变成「9月19日 周六 · 补记」

### 4.3 学习计划 `#/study`

顶部三个页签：**方向总览 / 学习项 / 学习记录**。

```
学习计划
├── Tabs：方向总览 | 学习项 | 学习记录
│
├── [页签A] 方向总览
│   └── 卡片网格，每卡：方向名 | 目标一句话 | 目标日期
│                     进度条 | 累计时长 | 剩余天数 | 状态标签
│
├── [页签B] 学习项（表格）
│   └── 列：名称 | 所属方向 | 类型 | 进度 | 状态 | 开始日 | 预计完成
│       行操作：编辑 / 删除 / 记一笔
│
└── [页签C] 学习记录
    └── 按日期倒序的流水，每条：日期 | 学习项 | 时长 | 笔记（可展开）
        顶部有「＋ 记一笔」按钮
```

交互点：

- 点方向卡片 → 直接过滤到该方向下的学习项
- 进度改成点击数字 + 拖动滑块两种方式都支持
- 时长输入接受三种写法：`90`、`1.5h`、`1h30m`，自动换算成分钟
- 学习项详情里能看到挂在这个项下的所有笔记，这是「笔记沉淀」的落点
- 记一笔时如果选了学习项，会问一句「进度要 +2% 吗」，可跳过（默认不自动改，避免失真）

### 4.4 开发工作 `#/dev`

顶部三个页签：**项目 / Bug 记录 / 踩坑笔记**。

```
开发工作
├── [页签A] 项目 → 点进 → 项目详情
│   └── 三列看板：待办 | 进行中 | 已完成
│       ├── 列头显示计数
│       ├── 任务卡可跨列拖动改状态
│       └── 右上「＋ 新增任务」
│
├── [页签B] Bug 记录（表格）
│   └── 列：标题 | 项目 | 严重级别 | 状态 | 创建时间
│       行操作：编辑 / 转成任务 / 删除
│
└── [页签C] 踩坑笔记
    └── 左列表 + 右正文（Markdown 渲染）
        顶部：标签筛选 + 全文搜索
```

交互点：

- 拖任务卡到「已完成」列时，自动盖一个完成时间
- Bug「转成任务」→ 生成一张任务卡落到该项目「待办」列，Bug 状态同步变「已解决」
- 踩坑笔记和 Bug 分开：Bug 是「现在要修的」，笔记是「以后别再踩的」
- 项目卡片上显示 `任务完成比` 和 `最近更新时间`，一眼看出哪个项目在动

### 4.5 咨询工作 `#/consult`

顶部两个页签：**客户 / 工时**。

```
咨询工作
├── [页签A] 客户
│   ├── 客户卡片：客户名 | 联系人 | 合作状态 | 累计工时 | 累计金额
│   └── 点进客户详情
│       ├── 项目块：项目名 | 周期 | 报价 | 阶段
│       ├── 交付物块：名称 | 状态 | 交付日期 | 链接
│       └── 纪要时间线：日期 | 参与人 | 要点 | 待跟进（可一键加进今日计划）
│
└── [页签B] 工时
    ├── 顶部计时器：项目下拉 + 开始 / 停止，运行时实时显示计时
    ├── 左：工时记录列表（日期 | 项目 | 时长 | 工作内容）
    └── 右：汇总区，四组切换 —— 按客户 / 按项目 / 本周 / 本月
            每组是一列横向条形图
```

交互点：

- 计时器停止即自动生成一条工时记录，时长可再手改
- 忘记停计时器时，超过 4 小时会提示「还在计时，要停吗」
- 纪要里的「待跟进」勾一下 → 直接加进今日计划，并带上客户名标签
- 客户详情里可以手动补一段没记的工时

### 4.6 健身计划 `#/fitness`

上下两块：周计划模板 + 打卡与历史。

```
健身计划
├── 上：本周计划模板
│   └── 七行：星期一~日 | 练什么（就地编辑）| 休息日开关
│       顶部「把上周套用到本周」按钮
├── 中：今日打卡
│   └── 一个大按钮 + 今日内容提示 + 连续打卡天数
└── 下：打卡历史
    └── 月历热力图：练了=深色格，没练=浅色格，休息日=灰色格
        点某天可补记或取消
```

交互点：

- 周计划是模板，跟打卡记录分开存：改了周计划，历史打卡不受影响
- 连续天数只统计「计划内且完成」的天，休息日不断连
- 热力图可切换月份，往回看这个月坚持得怎么样

### 4.7 饮食计划 `#/diet`

```
饮食计划
├── 顶部日期切换（← 9月20日 周日 →），可往前几天补记
├── 今日四格：早餐 | 午餐 | 晚餐 | 加餐
│   每格：内容输入框 + 完成勾选 +「从常用选」下拉
├── 常用项面板：增删常吃的东西，点一下就填进当前格
└── 下方历史：按日期倒序的折叠列表，展开看当天四格
```

交互点：

- 每格填了内容就自动勾上，也可以手动取消勾
- 常用项支持排序，常用的排前面
- 不做热量、不做营养，保持轻

### 4.8 游戏娱乐 `#/game`

顶部三个页签：**游戏库 / 时长记录 / 统计**。

```
游戏娱乐
├── [页签A] 游戏库
│   ├── 卡片 / 表格 两种模式切换
│   └── 按状态分组：在玩（置顶）| 想玩 | 已通关 | 弃坑
│       每款：名称 | 平台 | 评分 | 短感想
├── [页签B] 时长记录
│   ├── 顶部快速记录条：游戏下拉 + 分钟 + 回车即存
│   └── 列表：日期 | 游戏 | 时长 | 备注
└── [页签C] 统计
    └── 本周 / 本月切换 → 各游戏时长排行（横向条形图）+ 总时长
```

交互点：

- 快速记录条支持连加：`+30` 就在最后一条上加 30 分钟
- 游戏状态从「在玩」改成「已通关」时，自动记一个通关日期
- 评分用 1–10 的点击式刻度，不做星星

### 4.9 数据与设置 `#/settings`

```
数据与设置
├── 数据备份
│   ├── 「导出全部数据」→ 下载 JSON，显示文件名与体积
│   ├── 「导入数据」→ 选文件 → 预览条数 → 选覆盖 / 合并 → 二次确认
│   └── 上次备份时间
├── 存储占用
│   └── 各类数据条数 + 估算容量
├── 外观
│   ├── 主题：跟随时间 / 白天 / 晚上
│   └── 白天时段起止（默认 6:00 – 18:00）
├── 偏好
│   ├── 一周第一天：周一 / 周日
│   └── 打开默认进哪一页
├── 危险区
│   └── 清空全部数据（需输入「确认清空」）
└── 关于
    └── 版本号 + 各模块数据量统计
```

交互点：

- 导入前一定先给「预览」：会新增多少条、覆盖多少条
- 合并模式按 id 去重，同 id 以文件里的为准
- 清空后自动导出一次快照存到本地，作为最后一道保险

---

## 5. 跨模块联动（明确清单）

只有这五条，不做深度耦合。

| 起点 | 动作 | 终点 |
| --- | --- | --- |
| 咨询工作 · 沟通纪要 | 待跟进一键加进今日 | 今日计划里新增一条，带客户名标签 |
| 开发工作 · Bug | 转成任务 | 该项目看板「待办」列新增任务卡 |
| 学习 / 工时 / 游戏 | 记一笔 | 今日页右栏「今日记录」实时出现 |
| 今日计划条目 | 关联模块 | 对应模块里能看到这条的来源痕迹 |
| 所有模块 | 数据变化 | 首页对应摘要卡数值重算 |

---

## 6. 主题与响应式

**主题**
- 用 `<html data-theme="light|dark">` 切换，所有颜色走 CSS 变量，改一处全站生效
- 白天 6:00–18:00 自动用浅色，其余时间深色；手动锁定后不再自动跟随
- 两套配色分别调校：深色下不是简单反色，文字对比度、卡片层次、条形图配色都单独定

**响应式断点**

| 宽度 | 布局 |
| --- | --- |
| ≥ 1440px | 三栏，侧边栏展开 |
| 1024 – 1440px | 两栏，侧边栏展开 |
| < 1024px | 单栏，侧边栏收成图标条 |

手机端不做，窄屏只是保证窗口缩小的时候还能用。

---

## 7. 确认这份之后

这份结构定了，就可以进入编码：先搭外壳（侧边栏 + 路由 + 主题 + 存储层），再把九个模块一个个填进去。建议顺序是 **今日 → 学习计划 → 开发工作 → 咨询工作 → 健身 / 饮食 → 游戏 → 数据设置 → 首页总览**，首页放最后做，因为它的数据都来自其他模块。

---

## 8. 需要你确认的三点

1. 「今日」页左中右三栏的比例（5 : 3.5 : 3）合不合适，快速备忘会不会太小
2. 开发工作用**看板拖拽**，还是更想要一张普通任务表格
3. 导入数据默认给「覆盖」还是「合并」，还是每次让你选
