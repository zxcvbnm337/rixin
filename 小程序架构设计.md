# 微信小程序 · 架构设计（v1 草案）

> 目标：把「个人工作生活管理台」搬到微信小程序并上架。
> 状态：**待确认**。文中标注 ❓ 的地方需要你拍板后再动代码。

---

## 0. 先说结论：不能做 1:1 搬运

现有项目的架构假设和小程序**正好相反**：

| 维度 | 现在的网页版 | 微信小程序 |
|---|---|---|
| 运行位置 | 自己电脑浏览器，双击 index.html | 微信里，手机 |
| 数据存放 | IndexedDB / localStorage（本机） | 要么本机（换手机就丢），要么云 |
| 登录 | 没有 | 天然有 OpenID |
| 网络 | 完全不联网 | 必须联网（用云能力时） |
| 手机端 | PRD 明确「不做」 | 就是手机端 |
| 模块数 | 9 个，全靠手工录入 | 手机上打字慢，录入越多越劝退 |

**三个推不动的结论：**

1. **9 个模块不能照搬。** 手机输入效率远低于键盘，"先建项目/客户/学习方向再录入"的冷启动在小程序上必死。小程序只适合 1–2 个高频价值点做深。
2. **数据必须上云。** 纯本机存储意味着用户换手机、清缓存就全丢——没人敢把真实数据交给这样的工具。要么上云开发，要么这产品不成立。
3. **AI 是合规风险点，不是免费午餐。** 见第 2 节。

---

## 1. 上架前的三道硬门槛（已核实）

### 门槛一：ICP 备案 —— 关键路径，7~20 个工作日

按工信部规定，小程序**上架前必须完成 ICP 备案**，未备案无法发布。个人主体可以备案，但审核更严、周期约 **7–20 个工作日**（不是几天）。

> ⚠️ 这是"快速上线"里最大的变量，且**与技术无关**。
> 好消息：备案可以和开发**并行**。建议今天就去注册账号并提交备案，我这边同时开发。

### 门槛二：个人主体的能力边界

| 能力 | 个人主体 | 说明 |
|---|---|---|
| 云开发 CloudBase | ✅ 可开通 | 免费额度：数据库 2GB、读 5 万/天、写 3 万/天、存储 5GB、云函数并发 20 |
| 工具类目（备忘录/办公/记账/健康/计算/查询） | ✅ | 本产品落在「工具 > 实用工具 > 备忘录 / 办公」 |
| 社交 / 社区 / 电商 / 资讯 | ❌ 禁止 | 不要往这个方向设计 |
| 微信支付 | ❌ 不可开通 | 变现只能靠流量主广告。*另：2026-09 有消息称「虚拟支付」对个人主体在工具类目放开、月限额 10 万，需以 MP 后台实际入口为准* |
| **生成式 AI（AI 问答 / 生图 / 换脸）** | ❌ 未开放类目 | 官方社区已有裁定，个人主体做这些常被驳回 |

### 门槛三：AI 类目 —— 直接决定「AI 总结」能不能做

这是最需要你决策的一点：

- **纯个人主体**：做"AI 生成内容"（问答、总结、生图）有被驳回风险。可以尝试把它定位成**「结构化归纳」而非「生成式服务」**，但不保证过审。
- **个体工商户主体**：有营业执照 → 可申请更多类目、可申请深度合成类目，AI 功能名正言顺。成本是办执照 + 微信认证 300 元/次。

> ❓ **决策点 A：用哪种主体？**
> - 方案 A1：个人主体 —— 快、零成本、但 AI 功能受限
> - 方案 A2：个体工商户 —— 多花几天和一点钱，AI 与类目都宽松（**推荐**，如果 AI 是核心卖点）

---

## 2. 产品形态：做减法，只留一个价值点

小程序版本不做"9 个模块的管理台"，只做一件事：

> **用一句话记下你干了什么，自动归类成时间账本，每周给你一份复盘。**

为什么是这个：

- **手机原生**：一句话/语音录入，5 秒完成，符合小程序"即用即走"。
- **AI 用在刀刃上**：把「一句话 → 结构化条目」的解析交给 AI，这才是真正降低录入成本（网页版最大的问题就是录入太繁）。
- **有回报**：时间账本 + 周复盘是用户自己算不出来的东西。
- **可传播**：周报卡片天然适合分享。

### 页面结构（3 个 tab）

```
tabBar
├── 今天   pages/index   一句话录入 + 今日流水 + 三类时间占比
├── 账本   pages/ledger  周/月时间分配、趋势；进入「AI 周复盘」二级页
└── 我的   pages/me      目标设定、数据导出、隐私说明、关于
```

对比现有的 9 个模块，这里砍掉了：学习计划、开发工作、咨询工作、健身计划、饮食计划、游戏娱乐的**独立页面**——它们退化成 `records.kind` 的取值，靠统一的时间账本呈现。

> ❓ **决策点 B：接受这个减法吗？**
> - 接受 → 小程序是一个新产品，网页版继续做私人工具，两边各司其职
> - 不接受、要 9 个模块 → 可以，但我要提前说明：手机端留存会很难看

---

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | **原生小程序**（WXML/WXSS/JS） | 零构建、心智与现有项目一致、不引编译黑盒；现有 `core/util.js` 可近乎直接复用 |
| 云 | **微信云开发 CloudBase** | 免服务器、免备案域名、OpenID 天然可用、免费额度够 MVP |
| 状态 | 页面 data + 轻量 store 单例 | 与原项目 `App.store` 的思路一致 |
| 构建 | **无** | 不引 npm 依赖，避免 node_modules 与构建步骤 |

---

## 4. 目录结构

```
miniprogram/                    # 小程序工程根（新增，与网页版共存于同一仓库）
├── project.config.json         # appid / miniprogramRoot / compileType
├── project.private.config.json # 个人配置（gitignore）
├── app.js                      # wx.cloud.init、全局 store 引导
├── app.json                    # 页面注册、tabBar(custom)、窗口样式
├── app.wxss                    # 全局样式 + 设计变量（对齐现有两套主题思路）
├── pages/
│   ├── index/                  # 今天
│   ├── ledger/                 # 账本
│   ├── review/                 # AI 周复盘（二级页）
│   └── me/                     # 我的
├── components/
│   ├── quick-capture/          # 一句话录入（含语音）
│   ├── record-item/            # 条目行
│   ├── time-bar/               # 时间占比条
│   └── empty/                  # 空状态（沿用现有"引导文案 + 主操作按钮"规范）
├── core/
│   ├── util.js                 # ★ 从现有 js/core/util.js 迁移（纯函数，去掉 DOM 依赖）
│   ├── schema.js               # 精简后的枚举/常量
│   ├── store.js                # 本地缓存 + 云端同步 + 重试队列
│   └── api.js                  # 云函数调用封装
└── cloudfunctions/
    ├── parseRecord/            # 一句话 → 结构化条目（AI）
    ├── weeklyReview/           # 生成周复盘（AI）
    └── syncRecords/            # 拉取/合并记录
```

**复用说明**：现有 `js/core/util.js` 里的 `fmtDate / clamp / sum / weekdayIndexMon / addDays / pad2` 等是纯函数、无 DOM 依赖，可直接搬到小程序。现有的 15 个测试文件和 333 个用例是针对网页版的，**不能直接用于小程序**（jsdom + DOM 断言），需要为 `core/util.js` 单独写一套小程序侧单测。

---

## 5. 数据模型

现有网页版有 **23 个 storage key**（按模块分表）。小程序版**收敛为 3 个集合**：

```js
// records —— 统一记录表（核心）
{
  _id, _openid,
  date:    '2026-09-22',      // 归属日期，字符串便于范围查询
  kind:    'study',           // study | work | consult | fitness | diet | game | other
  title:   '看 RAG 文档',
  minutes: 60,                // 统一用分钟，展示时再格式化
  tags:    ['RAG'],
  source:  'ai',              // ai（一句话解析） | manual
  raw:     '上午看了一小时 RAG 文档',   // 原始输入，便于回溯与纠错
  createdAt, updatedAt
}

// reviews —— AI 周复盘缓存（避免每次重算）
{ _id, _openid, week: '2026-W39', summary, stats, createdAt }

// users —— 用户设置
{ _id, _openid, targets: { study: 300, work: 2400 }, createdAt }
```

**为什么要合并成一张表**：现在"按模块分表"导致跨模块聚合很痛苦（首页要读 9 个 key）。统一成 `records` 后，"这周时间怎么分配的"就是一次带 `where` 的查询——这正是账本与周复盘的地基。

**索引**：`_openid + date` 建复合索引，`kind` 建单列索引。云开发数据库按 `_openid` 天然隔离，无需额外权限设计。

---

## 6. 数据流与同步

```
        用户输入一句话
              │
              ▼
   ┌─────────────────────┐
   │ 1. 先写本地 storage │  ← 立即反馈，不等网络
   └──────────┬──────────┘
              ▼
   ┌─────────────────────┐
   │ 2. 调 parseRecord   │  ← AI 解析成结构化条目
   └──────────┬──────────┘
              ▼
   ┌─────────────────────┐
   │ 3. 写云数据库       │  ← 失败则进重试队列
   └──────────┬──────────┘
              ▼
        列表刷新（本地优先渲染）
```

- **读**：先渲染本地缓存 → 后台拉云数据按 `updatedAt` 合并 → 二次渲染。
- **首次启动**：`wx.cloud.callFunction` 换取 OpenID，建立 `users` 记录。
- **离线**：写入进本地队列，`onShow` / 网络恢复时补传。
- **导出**：复用网页版「导出 JSON」的思路，走 `wx.saveFileToDisk` 或复制到剪贴板。

---

## 7. 上架路线图（关键路径已标出）

| 步骤 | 谁做 | 预计耗时 |
|---|---|---|
| 1. 注册小程序账号、选主体 | 你 | 当天 |
| 2. 微信认证（个体户需做，300 元/次） | 你 | 1–5 工作日 |
| 3. 设置名称 / 头像 / 简介 / **服务类目** | 你 | 当天 |
| 4. **ICP 备案** ⚠️ **关键路径** | 你 | **7–20 工作日** |
| 5. 填「用户隐私保护指引」 | 你 | 当天 |
| 6. 开发 + 真机测试 | 我 | 并行进行 |
| 7. 上传代码 → 提交审核 | 我+你 | 1–3 工作日 |
| 8. 审核通过 → 手动发布 | 你 | 当天 |

> **最关键的一条建议：第 1–4 步今天就开始，不要等代码写完。**
> 备案是纯等待，和开发完全可以并行；等开发完再启动备案，会白白多等两三周。

### 开发侧还需要准备
- 安装**微信开发者工具**（本机当前未安装）
- 拿到 **AppID**（注册后从 MP 后台「开发管理 → 开发设置」获取）
- 云开发环境 ID（开通云开发后生成）

---

## 8. 风险与兜底

| 风险 | 影响 | 兜底 |
|---|---|---|
| AI 类目被驳回 | 核心功能上不了架 | 转个体工商户主体；或把 AI 降级为"本地规则解析 + 云端归纳"，弱化"生成"表述 |
| 备案超期 | 上线延后 | 无法加速，只能尽早启动；期间用**体验版**（≤100 人）给种子用户试用 |
| 个人主体无支付 | 无法收费 | 走流量主广告；或后续升企业主体 |
| 云开发免费额度用尽 | 服务中断 | 早期额度充裕（2GB / 5 万读/天）；设用量告警 |
| 用户留存低 | 获客无意义 | 靠「AI 周复盘」形成每周回访锚点 + 订阅消息提醒 |

---

## 9. 分阶段实施

| 阶段 | 内容 | 产出 |
|---|---|---|
| P0 | 工程骨架：app.json / tabBar / 4 个页面空壳 / 设计变量 | 开发者工具里能跑起来 |
| P1 | `core/` 层：util 迁移 + store + 本地存储闭环 | 能录入、能在本地看到流水（纯离线可用） |
| P2 | 云开发接入：OpenID、records 集合、读写同步 | 换设备数据不丢 |
| P3 | AI 接入：`parseRecord` 一句话解析 | 核心体验成立 |
| P4 | 账本 + `weeklyReview` 周复盘 | 形成"每周回访"钩子 |
| P5 | 打磨：空状态、分享卡片、隐私指引、真机适配 | 可提交审核 |
| P6 | 上架：上传 → 审核 → 发布 | 正式上线 |

P0–P1 不依赖 AppID 和备案，**可以立刻开始**（用测试号或以后填 AppID）。

---

## 10. 待你拍板

- ❓ **决策点 A**：主体用**个人**还是**个体工商户**？（决定 AI 功能能否名正言顺）
- ❓ **决策点 B**：接受"做减法、只做时间账本 + AI 复盘"，还是坚持 9 个模块全搬？
- ❓ **决策点 C**：是否已注册小程序账号 / 拿到 AppID？没有的话先去注册并启动备案。
- ❓ **决策点 D**：小程序代码放 `D:\我的app\miniprogram\`（同仓库），还是单独建仓库？

---

## 11. 交付记录

### 已定（你的选择）
- 主体：**个人主体**
- 产品形态：**做减法** → 「一句话录入 → 时间账本 → 周复盘」
- 账号：**还没注册**

裸奔结论：**备案是唯一的硬等待（7–20 个工作日），必须今天就启动**；
AI 类目在个人主体下大概率过不了，所以产品**不把 AI 作为前置依赖**。

### P0 + P1 已交付（`miniprogram/`，可在开发者工具直接打开）

```
miniprogram/
├── app.js / app.json / app.wxss / sitemap.json / project.config.json
├── custom-tab-bar/index.{js,json,wxml,wxss}     自定义纯文字 tabBar
├── core/                                        ← 不依赖 wx / DOM，可用 node 直跑
│   ├── util.js      日期与时长纯函数（从网页版迁移）
│   ├── schema.js    6 个分类枚举、存储 key、默认设置、云环境占位
│   ├── store.js     本地优先数据层 + 待同步队列 + 撤销 + 导入导出
│   └── review.js    本地规则版周复盘（无 AI 也有交付物）
└── pages/
    ├── index/   今天：一句话录入 + 今日清单 + 断档提醒
    ├── ledger/  账本：7/30 天柱状图 + 分类占比 + 分类筛选明细
    ├── me/      我的：概览、周起始日、AI 开关、导出/导入/清空、撤销
    └── review/  周复盘：结论 + 建议 + 环比，可翻历史周
```

关键设计取舍：

| 决定 | 原因 |
|---|---|
| **本地优先**：写入先落本机，云端是后台行为 | 没有云开发 / 断网 / 未登录时全链路可用；小程序能过审、能上线 |
| **AI 可插拔**：本地规则复盘为默认，云端大模型是选项 | 个人主体 AI 类目未开放，产品不能把上线押在审核上 |
| **分类 6 个**（学习/工作/运动/饮食/娱乐/其他） | 网页版 9 模块录一次要选半天，这是「繁琐」的根源之一 |
| **保留 `LEGACY_KIND_MAP`** | 网页版 dev/consult→工作、game→娱乐，历史数据能直接导入 |
| **不接分享/转发/支付/登录** | 全是个人主体或用不上的能力，减少审核面 |
| tabBar 纯文字、无图标 | 省两套切图与选中态维护 |

### 测试基建（不依赖微信开发者工具）

沿用网页版的「写文件再读」方案，新增两个文件进全量回归：

- `tests/16-mp-core.test.js`（72 例）— core 层逻辑：解析、统计、软删除与撤销、
  待同步队列、连续天数、导入导出、复盘规则全分支。
- `tests/17-mp-project.test.js`（15 例）— 工程结构静态校验：
  页面文件完整性、tabBar 指向、**WXML 事件处理器是否真有实现**（最常见的「点了没反应」）、
  WXML 标签闭合、require 路径、core 层是否误用 `wx`/`document`、
  深浅两套主题变量是否一一对应、是否误引入支付/登录/转发。

当前：**17 个文件 / 421 个用例 / 0 失败 / 0 跳过**。
顺带修掉了两个历史遗留的**日期依赖缺陷**（`09-consult` 假设「20 天前必在上月」、
`12-game` 用固定参照日造数据却按真实今天断言 UI）——这类测试在月初/月末会随机变红。

### 你下一步要做的三件事（按优先级）

1. **今天就去注册小程序账号 + 启动 ICP 备案**（关键路径 7–20 个工作日，无法加速）。
   入口：mp.weixin.qq.com → 注册 → 个人主体 → 实名。
   服务类目建议：**工具 > 实用工具 > 备忘录**（或 效率 > 笔记）。
2. **装微信开发者工具**，导入 `D:\我的app\miniprogram`（AppID 先用「测试号」）。
   现在就能跑：录入 → 账本 → 周复盘整条链路都是通的。
3. 拿到 AppID 后替换 `project.config.json` 里的 `touristappid`。

备案期间可以先用**体验版**（≤100 人）给种子用户试用，不必等正式发布。

### 下一步可选（P2/P3）
- P2 云开发：`CLOUD_ENV` 填值 → 打开 `app.js` 里注释的 `wx.cloud.init` →
  云函数 `syncRecords` 消费 `store.pending()` 队列（队列机制已就绪，`flushPending()` 待接）。
- P3 AI：`core/parse.js` 本地正则兜底（"看了 1.5 小时书" → 学习/90 分钟），
  云端可选接大模型；开关与文案已就位，用户侧无感升级。

