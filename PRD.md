# 个人工作生活管理 App · PRD（第一版）

> 版本：v1.0　状态：**已确认，作为开发依据**　日期：2026-09-20
> 配套文档：`个人管理APP-方案-v1.md`（字段细节）、`个人管理APP-页面结构与交互设计.md`（结构细节）
> 本文档为最终依据，与前两份文档冲突时，以本文档为准。

---

## 1. 产品目标和使用场景

### 1.1 目标

做一个**只在自己电脑浏览器里运行的个人管理台**，把三块工作（学习、开发、咨询）、三类生活（健身、饮食、游戏）和每天的待办收进同一个界面，替代目前散落的便签、记事本和 Excel。

一句话：**打开电脑就能看到今天要干什么，随手能记，事后能查。**

### 1.2 硬约束

| 项 | 要求 |
| --- | --- |
| 使用者 | 只有我自己，单机单人 |
| 运行环境 | 只在我这台电脑的浏览器里跑，双击文件即打开 |
| 网络 | 完全不需要联网，断开网也能用全部功能 |
| 账号 | 不需要登录，不做用户体系 |
| 数据 | 全部存在本机，不上传任何数据到任何地方 |
| 端 | 只做电脑网页，不做手机端 |
| 部署 | 不需要部署、不需要起服务、不需要安装东西 |

### 1.3 典型使用场景

1. **早上开工**：双击打开，首页仪表盘扫一眼——今天几条待办、健身饮食打没打卡、学习进度到哪了、手上有几个项目在动。
2. **白天随手记**：想到一件事，敲进「今日计划」或「快速备忘」，不打断手上的活。
3. **咨询开会**：点开始计时，会开完点停止，自动生成一条工时记录，月底不用回忆。
4. **开发踩坑**：把坑写进踩坑笔记，打个标签，下次遇到直接搜。
5. **晚上收尾**：记今天学了什么、玩了多久游戏，勾掉健身和饮食打卡。
6. **每周复盘**：看工时按项目怎么分的、学习投入多少小时、游戏时间花在哪。

---

## 2. 整体页面框架和导航结构

### 2.1 应用外壳

单页应用，十个页面共用一层外壳，切换靠 hash 路由，**不刷新浏览器**。

```
Sidebar 侧边栏（固定 200px）
├── 应用名
├── 导航组（4 组 10 项）
└── 底部：数据条数摘要 + 主题切换

Main 主区
├── Topbar 顶栏
│   ├── 左：当前页面标题 + 一句话说明
│   ├── 中：全局搜索（搜任务 / 笔记 / 游戏 / 客户 / 项目）
│   └── 右：快捷新增 + 主题切换
└── PageHost 内容区

Overlay 浮层
├── Drawer 右侧抽屉（复杂表单）
├── Confirm 二次确认框（删除等危险操作）
└── Toast 右下角提示（3 秒消失，带撤销）
```

### 2.2 侧边栏分组

```
首页总览
今日
────────────
学习计划
开发工作
咨询工作
────────────
健身计划
饮食计划
游戏娱乐
────────────
数据与设置
```

### 2.3 路由表

| 路由 | 页面 |
| --- | --- |
| `#/home` | 首页总览 |
| `#/today` | 今日 |
| `#/study` | 学习计划 |
| `#/dev` | 开发工作 |
| `#/dev/project/:id` | 项目详情（看板） |
| `#/consult` | 咨询工作 |
| `#/consult/client/:id` | 客户详情 |
| `#/fitness` | 健身计划 |
| `#/diet` | 饮食计划 |
| `#/game` | 游戏娱乐 |
| `#/settings` | 数据与设置 |

### 2.4 全局交互规则（十条）

| # | 规则 |
| --- | --- |
| 1 | 简单条目用行内输入框回车新建；超过 3 个字段的表单走右侧抽屉 |
| 2 | 点击文字就地变输入框，失焦自动保存，Esc 取消 |
| 3 | 删除一律先确认；删完 Toast 带「撤销」，5 秒内可恢复 |
| 4 | 三处拖拽：今日计划排序、开发看板换状态、健身周计划排序 |
| 5 | 没有「保存」按钮，任何改动即时落库 |
| 6 | 快捷键：`/` 聚焦搜索、`N` 新增、`Ctrl+1~9` 切页面、`Esc` 关浮层 |
| 7 | 每个列表都有空状态引导文案 + 一个主操作按钮 |
| 8 | 列表页顶部统一一行筛选条：状态 / 日期范围 / 关键词 |
| 9 | 首页摘要每次切页重算，不需要手动刷新 |
| 10 | 清空数据需输入「确认清空」四个字才能执行 |

### 2.5 主题与响应式

**主题**：白天（浅色）/ 晚上（深色）两套，用 `data-theme` 切换，所有颜色走 CSS 变量。
默认 6:00–18:00 用白天、其余用晚上；手动锁定后不再自动跟随。深色配色单独调校，不是简单反色。

| 窗口宽度 | 布局 |
| --- | --- |
| ≥1440px | 三栏，侧边栏展开 |
| 1024–1440px | 两栏，侧边栏展开 |
| <1024px | 单栏，侧边栏收成图标条 |

---

## 3. 首页的内容结构

### 3.1 定位

**只做摘要，不做录入。** 除勾选完成和写便签两处外，首页不产生任何新数据。它的作用是让我在 3 秒内知道今天该关注什么。

### 3.2 卡片排布

```
行1：今日计划  |  今日打卡  |  学习进度
行2：项目进展  |  本周工时
行3：游戏时长  |  快速备忘
```

### 3.3 每张卡片的内容与点击行为

| 卡片 | 显示内容 | 就地可操作 | 点击去向 |
| --- | --- | --- | --- |
| 今日计划 | 今天未完成的待办，最多 5 条 | 直接勾选完成 | → 今日 |
| 今日打卡 | 健身 / 饮食今天的状态 | 一键补打卡 | → 健身 / 饮食 |
| 学习进度 | 进行中的学习项 + 进度条，最多 3 条 | 无 | → 学习计划 |
| 项目进展 | 进行中的开发 / 咨询项目 + 任务完成比 | 无 | → 对应模块 |
| 本周工时 | 本周累计工时，按项目分条 | 无 | → 咨询工作 |
| 游戏时长 | 本周累计时长 + 最近在玩 | 无 | → 游戏娱乐 |
| 快速备忘 | 最近的便签 | 就地编辑、新增、删除、置顶 | → 今日 |

卡片悬停时右上角浮出「…」菜单：进入模块 / 隐藏此卡。第一版卡片顺序固定，不做拖拽排序。

---

## 4. 每个模块的用途

| 模块 | 用途（一句话） | 更具体的说明 |
| --- | --- | --- |
| **首页总览** | 每天第一眼看的地方 | 把各模块的关键摘要聚到一屏，只读为主，用来判断今天该把注意力放哪 |
| **今日** | 每天的执行入口 | 今天要做的事、要记的备忘、要打的卡都在这；是全 App 唯一横跨所有模块的页面 |
| **学习计划** | 技能学习台账 | 主线是「方向 → 学习项 → 学习记录」，解决"学了很多但要回忆时说不清学到哪" |
| **开发工作** | 工程项目台账 | 主线是「项目 → 任务 → 状态流转」。Bug 是"现在要修的"，踩坑笔记是"以后别再踩的"，两者分开 |
| **咨询工作** | 客户交付台账 | 主线是「客户 → 项目 → 交付 + 工时」。重点解决工时记不住、月底算不清的问题 |
| **健身计划** | 轻量打卡 | 排好每周练什么，做完打勾，看连续坚持天数。不记重量、组数、次数 |
| **饮食计划** | 轻量打卡 | 记三餐吃了什么，打勾表示按计划吃了。不算热量、不做营养分析 |
| **游戏娱乐** | 游戏库 + 时长 | 记录想玩 / 在玩 / 通关的状态和感想，同时统计游戏时间花在哪 |
| **数据与设置** | 数据兜底 + 个人偏好 | 导出 / 导入备份、主题、周起始日等；是数据不丢的最后一道保险 |

---

## 5. 各模块第一版最必要的数据和操作

### 5.1 今日

**数据**
- 今日计划条目：标题、日期、优先级（高 / 中 / 低）、完成状态、完成时间、关联模块、备注
- 备忘便签：内容、是否置顶、创建时间
- 打卡状态：健身（完成 / 未完成）、饮食（早 / 午 / 晚 / 加餐 各自完成状态 + 内容）

**操作**
- 输入框回车新增计划条目；勾选完成 / 取消完成
- 就地编辑标题；拖拽排序
- 「顺延到明天」「移到其他模块」「删除」
- 新增 / 编辑 / 置顶 / 删除便签
- 一键健身打卡与取消；逐个勾选饮食四餐
- 切到昨天（或更早）补记

### 5.2 学习计划

**数据**
- 学习方向：名称、目标描述、目标日期、状态（进行中 / 已达成 / 已归档）
- 学习项：名称、所属方向、类型（课程 / 书 / 文档 / 视频 / 其他）、链接、进度%、状态（未开始 / 进行中 / 已完成 / 搁置）、开始日期、预计完成日期
- 学习记录：日期、学习项、时长（分钟）、笔记内容

**操作**
- 新增 / 编辑 / 归档学习方向
- 新增 / 编辑 / 删除学习项；修改进度（点数字或拖滑块）
- 记一笔学习（选学习项 + 时长 + 笔记），时长支持 `90` / `1.5h` / `1h30m` 三种写法
- 按方向筛选学习项；按学习项聚合查看笔记
- 看各方向累计时长与进度

### 5.3 开发工作

**数据**
- 项目：名称、描述、技术栈标签、状态（进行中 / 暂停 / 已完成）、创建时间、最近更新
- 任务：标题、描述、所属项目、优先级、状态（待办 / 进行中 / 已完成）、标签、创建时间、完成时间
- Bug：标题、所属项目、严重级别、状态（待修 / 修复中 / 已解决）、复现步骤、解决方式
- 踩坑笔记：标题、正文（Markdown）、标签、创建时间

**操作**
- 新增 / 编辑 / 删除项目；点进项目看板
- 在项目看板三列中拖拽任务改状态；拖到「已完成」自动盖完成时间
- 新增 / 编辑 / 删除任务
- Bug 记录新增 / 编辑 / 删除；「转成任务」→ 生成任务卡落到该项目待办列，Bug 状态同步改为已解决
- 踩坑笔记新增 / 编辑 / 删除，标签筛选 + 全文搜索

### 5.4 咨询工作

**数据**
- 客户：名称、联系人、联系方式、合作状态（洽谈中 / 进行中 / 已结束）、备注
- 项目：名称、所属客户、开始日期、结束日期、报价、阶段
- 交付物：名称、所属项目、状态（待交付 / 已交付）、交付日期、链接
- 沟通纪要：日期、客户、项目、参与人、要点、待跟进
- 工时记录：日期、项目、时长（分钟）、工作内容

**操作**
- 客户新增 / 编辑 / 删除；点进客户详情
- 项目管理与交付物清单维护
- 写沟通纪要；「待跟进」一键加进今日计划
- 工时记录新增 / 编辑 / 删除
- 计时器：选项目 → 开始 → 停止 → 自动生成一条工时记录（时长可再手改）；超过 4 小时提示「还在计时，要停吗」
- 工时汇总：按客户 / 按项目 / 本周 / 本月 四组切换，横向条形图

### 5.5 健身计划

**数据**
- 周计划行：星期、内容（自由文本）、是否休息日
- 打卡记录：日期、是否完成、备注

**操作**
- 编辑每周练什么；标记某天为休息日；「把上周套用到本周」
- 今日一键打卡 / 取消打卡
- 月历热力图查看打卡情况，点某天可补记或取消
- 查看连续打卡天数（休息日不断连）

### 5.6 饮食计划

**数据**
- 餐饮记录：日期、餐次（早 / 午 / 晚 / 加餐）、内容、是否完成、备注
- 常用项：常吃的东西列表

**操作**
- 填四餐内容，填了自动勾上，也可手动取消
- 「从常用选」快速填入；常用项增删排序
- 顶部日期切换，往前几天补记
- 按日期倒序查看历史

### 5.7 游戏娱乐

**数据**
- 游戏：名称、平台（PC / 主机 / 手机）、状态（想玩 / 在玩 / 已通关 / 弃坑）、评分（1–10）、感想、加入时间、通关时间
- 时长记录：日期、游戏、时长（分钟）、备注

**操作**
- 游戏新增 / 编辑 / 删除；卡片与表格两种查看模式
- 快速记录时长：选游戏 + 分钟 + 回车；`+30` 在最后一条上加 30 分钟
- 状态改为「已通关」时自动记通关日期
- 统计页看本周 / 本月各游戏时长排行与总时长

### 5.8 数据与设置

**数据**
- 设置项：主题模式、白天时段起止、一周第一天、打开默认进哪一页、上次备份时间

**操作**
- 导出全部数据为 JSON 文件；导入 JSON
- 查看存储占用与各类数据条数
- 切换主题（跟随时间 / 白天 / 晚上）、设置白天时段
- 设置周起始日、默认首页
- 清空全部数据（需输入「确认清空」）

---

## 6. 首页、今日计划和各模块之间的关系

### 6.1 三层分工

| 层 | 角色 | 数据权限 |
| --- | --- | --- |
| **首页总览** | 看板层 | **只读聚合**。只读各模块数据做展示，唯一例外是勾选完成和写便签 |
| **今日** | 执行层 | **唯一的横跨层**。今天要做的事在这里汇总，可以来自任何模块，也可以什么都不来自 |
| **九个功能模块** | 归属层 | 数据的**正式归属地**。完整记录、完整字段都在模块内，不会因为今日页删除而丢失 |

### 6.2 核心规则

1. **数据只有一个家**：每条数据只属于一个模块。首页和今日页看到的都是引用或镜像，不做第二份存储。
2. **今日计划是唯一的跨模块中转站**：跨模块流转都经由今日计划，模块之间不直接互相写数据。
3. **首页不产生数据**：除便签和勾选状态外，首页只读不写，避免出现第二份记录。
4. **首页永远是最新的**：任何模块数据变化，切回首页即重算，不需要刷新。

### 6.3 五条联动

| 起点 | 动作 | 终点 |
| --- | --- | --- |
| 咨询工作 · 沟通纪要 | 「待跟进」加进今日 | 今日计划新增一条，带客户名标签 |
| 开发工作 · Bug | 「转成任务」 | 该项目看板「待办」列新增任务卡 |
| 学习 / 工时 / 游戏 | 记一笔 | 今日页右栏「今日记录」实时出现 |
| 今日计划条目 | 关联模块 | 对应模块能看到这条的来源痕迹 |
| 任意模块 | 数据变化 | 首页对应摘要卡数值重算 |

### 6.4 数据流向

```
学习计划 ──┐
开发工作 ──┤
咨询工作 ──┼──→ 首页总览（只读聚合，各模块摘要卡）
健身计划 ──┤
饮食计划 ──┤
游戏娱乐 ──┘

咨询纪要「待跟进」──→ 今日计划 ──→ 各模块留痕
开发 Bug「转任务」──→ 项目看板待办列
学习 / 工时 / 游戏「记一笔」──→ 今日页右栏记录区
```

---

## 7. 本地数据、备份和恢复要求

### 7.1 数据存放在哪

| 项 | 要求 |
| --- | --- |
| 主存储 | 浏览器本地数据库，优先 IndexedDB；若浏览器在本地文件模式下受限，自动降级为 localStorage |
| 位置 | 只在本机，**不上传任何数据到任何服务器**，全程无网络请求 |
| 生命周期 | 刷新页面、关闭标签页、关闭浏览器、重启电脑，数据都不能丢 |
| 容量 | 设置页显示各类数据条数与估算占用；接近容量上限时给出提醒 |
| 风险 | 清除浏览器数据 / 换浏览器 / 用无痕模式会丢失，必须靠导出的备份文件兜底 |

### 7.2 备份（导出）

- 设置页「导出全部数据」→ 浏览器下载一个 JSON 文件
- 文件名格式：`个人管理-备份-20260920.json`
- 文件内包含：全部模块数据、设置项、导出时间、数据版本号
- 导出后记录「上次备份时间」
- 距上次备份超过 **7 天**时，顶部出现一条浅色提示条（不弹窗、可关闭，当天内不再出现）

### 7.3 恢复（导入）

- 设置页「导入数据」→ 选文件 → **先给预览**（将新增 X 条、覆盖 Y 条）→ **弹出选择框让我选「覆盖」还是「合并」**（每次都弹，不设默认）→ 二次确认后执行
- **覆盖**：清空现有数据，完全以文件为准
- **合并**：按 id 去重，同 id 的记录以文件里的为准，其余追加
- 导入完成后 Toast 提示结果，并自动刷新当前页面

### 7.4 兜底

- 执行「清空全部数据」前，自动先导出一份快照文件，作为最后一道保险
- 删除单条数据时 Toast 带撤销，5 秒内可恢复

---

## 8. 第一版必须完成的功能

### 8.1 基础框架

- [ ] 应用外壳：侧边栏（10 个入口，4 组分组）+ 顶栏 + 内容区
- [ ] Hash 路由，11 条路由全部可用，切换不刷新浏览器
- [ ] 白天 / 晚上两套主题，全站统一，默认按时间自动切换，可手动锁定
- [ ] 响应式：三档断点，窄屏侧边栏收成图标条
- [ ] 通用组件：卡片、列表行、看板列、标签、进度条、勾选框、条形图、热力图、抽屉、确认框、Toast、空状态、筛选条、日期切换、页签
- [ ] 全局交互：行内新增、就地编辑、删除可撤销、自动保存、快捷键

### 8.2 数据层

- [ ] 本地存储层（IndexedDB 优先 + localStorage 降级）
- [ ] 所有模块数据的增删改查即时落库
- [ ] 一键导出 JSON 备份
- [ ] 导入 JSON，带预览与「覆盖 / 合并」选择
- [ ] 清空数据前自动导出快照
- [ ] 距上次备份超 7 天的顶部提示条

### 8.3 各模块功能

- [ ] **今日**：今日计划（新增、勾选、编辑、排序、顺延、移动、删除）、快速备忘、健身饮食打卡、今日记录区、日期切换补记
- [ ] **学习计划**：方向总览、学习项表格、学习记录流水、笔记按学习项聚合、进度与时长统计
- [ ] **开发工作**：项目列表、项目看板（三列拖拽）、任务增删改、Bug 记录与转任务、踩坑笔记（Markdown + 标签 + 搜索）
- [ ] **咨询工作**：客户列表、客户详情（项目 / 交付物 / 纪要时间线）、纪要待跟进转今日、工时记录、计时器、四维度工时汇总条形图
- [ ] **健身计划**：周计划模板、今日打卡、连续天数、月历热力图、补记
- [ ] **饮食计划**：四餐记录与勾选、常用项、日期切换、历史查看
- [ ] **游戏娱乐**：游戏库（卡片 / 表格）、状态分组、评分、时长快速记录、本周本月排行
- [ ] **数据与设置**：备份与恢复、存储占用、外观、偏好、清空重置、关于
- [ ] **首页总览**：七张摘要卡、卡片跳转、勾选与便签就地操作、超 7 天备份提示

### 8.4 联动

- [ ] 五条跨模块联动全部打通

---

## 9. 第一版暂时不做的功能

| # | 不做的东西 | 原因 |
| --- | --- | --- |
| 1 | 登录、账号体系 | 单机自用，不需要 |
| 2 | 云同步、云数据库 | 明确不要联网 |
| 3 | 手机端 / 移动适配 | 只在电脑上用 |
| 4 | 部署上线、生成分享链接 | 本地双击打开即可 |
| 5 | 联网抓数据（自动拉平台数据、自动同步） | 无网络依赖 |
| 6 | 复杂报表、自定义图表、数据透视 | 第一版先把记录做扎实 |
| 7 | 提醒推送、桌面通知、定时提醒 | 不做系统集成 |
| 8 | 多设备 / 多人共享 | 单机单人 |
| 9 | 训练重量组数、次数记录 | 健身只做轻量打卡 |
| 10 | 热量、营养、宏量计算 | 饮食只做轻量打卡 |
| 11 | 密码锁、数据加密 | 本机自用 |
| 12 | 卡片拖拽排序、自定义首页布局 | 首页顺序固定 |
| 13 | 图片 / 附件上传（游戏封面、交付物截图） | 只存链接和文字 |
| 14 | 全局搜索的高级语法、模糊匹配 | 只做关键词包含匹配 |
| 15 | 数据统计的导出（Excel / PDF） | 需要时用截图 |

---

## 10. 验收标准

所有验收项都可实际动手检查，逐条勾。

### A. 启动与外壳

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| A1 | 双击即用 | 双击文件，浏览器打开 | 直接进入首页，控制台无报错 |
| A2 | 无需联网 | 断网后打开并操作 | 所有功能正常，控制台无任何网络请求 |
| A3 | 路由切换 | 依次点侧边栏 10 个入口 | 页面切换、地址栏 hash 变化、浏览器不刷新 |
| A4 | 响应式 | 把窗口从最大缩到 900px 宽 | 侧边栏收成图标条，内容变单栏，无横向滚动条 |

### B. 主题

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| B1 | 晚上主题 | 顶栏切到「晚上」 | 全部 10 个页面背景转深、文字转浅，无残留亮块 |
| B2 | 主题记忆 | 锁定「晚上」后刷新页面 | 主题保持晚上，不跳回白天 |
| B3 | 自动切换 | 把白天时段设为当前时间之外 | 打开 App 自动使用晚上主题 |

### C. 数据持久化（最关键）

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| C1 | 刷新不丢 | 新增 5 条今日计划 + 3 条便签，按 F5 | 5 条计划和 3 条便签原样还在 |
| C2 | 关页面不丢 | 关闭标签页，重新双击打开文件 | 数据全在 |
| C3 | 重启不丢 | 重启电脑后再打开 | 数据全在 |
| C4 | 各模块落库 | 每个模块各新增 1 条数据后刷新 | 九个模块的新增数据一条不少 |

### D. 模块功能

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| D1 | 今日计划 | 回车新增一条 → 勾选 → 拖拽排序 → 点「顺延到明天」 | 能建；勾选后变浅带删除线；拖拽生效；今天消失、明天的列表里出现 |
| D2 | 学习计划 | 建 1 个方向 + 2 个学习项，把进度改成 50%；记一笔 90 分钟 | 方向总览进度条同步变化；方向累计时长增加 1.5 小时 |
| D3 | 开发看板 | 建 1 个项目 + 3 个任务，把任务从「待办」拖到「已完成」 | 卡片出现在已完成列，且自动盖上完成时间 |
| D4 | Bug 转任务 | Bug 记录里点「转成任务」 | 该项目待办列多一张任务卡，Bug 状态变成「已解决」 |
| D5 | 咨询计时 | 选项目 → 开始 → 等 60 秒 → 停止 | 生成一条约 1 分钟的工时记录；右侧按项目汇总数字同步增加 |
| D6 | 工时汇总 | 切四组维度 | 按客户 / 按项目 / 本周 / 本月 四组数据都正确，条形图比例与数值一致 |
| D7 | 健身打卡 | 点一次打卡 | 连续天数从 0 变 1；热力图当天格子变色；再点一次取消，天数回落 |
| D8 | 饮食打卡 | 四格各填内容 | 填完自动勾上；切到昨天能看到昨天的记录 |
| D9 | 游戏时长 | 新增 1 款游戏，记 30 分钟 | 统计页本周排行里它排第一，总时长 0.5 小时 |
| D10 | 空状态 | 新数据下首次进入每个页面 | 十个页面都不空白，均有引导文案和主操作按钮 |
| D11 | 首页联动 | 在任意模块改数据后回首页 | 对应摘要卡数值同步变化，不用刷新 |

### E. 备份与恢复

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| E1 | 导出 | 点「导出全部数据」 | 下载到 `个人管理-备份-日期.json`，能正常打开、内容完整 |
| E2 | 恢复 | 清空浏览器数据 → 重开 App → 导入刚才的文件 | 数据全部回来，条数与导出时一致 |
| E3 | 导入选择 | 导入时观察 | 每次都弹出「覆盖 / 合并」选择框，两种都能跑通 |
| E4 | 导入预览 | 导入前 | 显示「将新增 X 条、覆盖 Y 条」的准确预览 |
| E5 | 合并去重 | 对同 id 记录做合并导入 | 不产生重复条目，以文件里的内容为准 |
| E6 | 备份提醒 | 把上次备份时间改成 8 天前 | 顶部出现提示条；关掉后当天不再出现 |
| E7 | 清空兜底 | 执行清空全部数据 | 清空前自动导出一份快照文件；清空需输入「确认清空」才可点 |

### F. 交互与容错

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| F1 | 删除撤销 | 删掉一条任务 | Toast 出现「撤销」，5 秒内点撤销能恢复 |
| F2 | 就近编辑 | 双击任意列表项文字 | 就地变输入框，失焦自动保存，Esc 取消不保存 |
| F3 | 快捷键 | 依次按 `/`、`N`、`Ctrl+3`、`Esc` | 分别聚焦搜索、触发新增、切到第三个页面、关闭浮层 |
| F4 | 筛选条 | 在列表页按状态 / 日期 / 关键词筛选 | 三种筛选都能生效，结果正确 |

### G. 性能

| 编号 | 验收项 | 怎么验 | 通过标准 |
| --- | --- | --- | --- |
| G1 | 数据量 | 造 5000 条数据（可用脚本或反复导入） | 页面切换、列表滚动不卡顿，切换耗时肉眼无感 |

---

## 附：开发顺序建议

1. 外壳：侧边栏 + 路由 + 主题 + 存储层
2. 今日
3. 学习计划
4. 开发工作
5. 咨询工作
6. 健身计划 / 饮食计划
7. 游戏娱乐
8. 数据与设置
9. 首页总览（放最后，因为它的数据全部来自其他模块）
