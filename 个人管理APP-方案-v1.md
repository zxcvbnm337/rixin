# 个人工作生活管理 App · 第一版方案

> 状态：**待确认**　版本：v1　日期：2026-09-20
> 确认后按这份方案开发，开发中不再改结构，只做细节打磨。

---

## 一、整体形态

| 项 | 方案 |
| --- | --- |
| 运行方式 | 本地网页，双击文件用浏览器打开即可。不装东西、不起服务、不联网、不登录 |
| 数据存放 | 浏览器本地数据库（优先 IndexedDB，若浏览器在本地文件模式下受限则自动降级 localStorage），全部存你这台电脑 |
| 数据安全 | 「数据与设置」提供一键导出 JSON 备份文件；导入支持「覆盖」或「合并」两种方式 |
| 屏幕适配 | 电脑宽屏优先，三栏布局；窗口变窄时自动收成单栏。不做手机版 |
| 主题 | 白天 / 晚上两套配色，默认按时间自动切换（6:00–18:00 白天），可在设置里手动锁定 |
| 语言 | 全中文 |

---

## 二、导航骨架

**左侧固定侧边栏**（带分组分隔线，从上到下）：

```
首页总览
今日
──────────
学习计划
开发工作
咨询工作
──────────
健身计划
饮食计划
游戏娱乐
──────────
数据与设置
```

**顶部条**：当前日期与星期 ｜ 全局搜索框 ｜ 主题切换按钮（自动 / 白天 / 晚上）

**全局交互约定**（所有页面统一）：

- 新增内容用行内输入框或右上角「+ 新增」按钮，回车 / 点击即提交，没有「保存」按钮
- 输入框失焦即自动存盘，不需要手动保存
- 删除一律先确认；重要数据删除后 5 秒内可撤销
- 每个页面的空状态给一句引导文案，不出现空白页
- 列表支持按状态 / 日期 / 关键词筛选

---

## 三、各页面方案

### 1. 首页总览（仪表盘）

**定位**：只做摘要，不在这里做重活。三栏卡片网格，窄屏自动变两栏、单栏。

| 卡片 | 显示内容 | 可操作 |
| --- | --- | --- |
| 今日计划 | 今天未完成的待办，最多 5 条 | 直接勾选完成；「查看全部」跳今日 |
| 今日打卡 | 健身 / 饮食今天打卡了没 | 一键补打卡 |
| 学习进度 | 进行中的学习项 + 进度条，最多 3 条 | 点进学习计划 |
| 项目进展 | 进行中的开发 / 咨询项目 + 任务完成比 | 点进对应模块 |
| 本周工时 | 本周累计工时，按项目分条 | 点进咨询工作 |
| 游戏时长 | 本周累计时长 + 最近在玩的游戏 | 点进游戏娱乐 |
| 快速备忘 | 最近的便签，可直接编辑 | 就地编辑、删除、置顶 |

点卡片标题 = 进入对应模块。卡片顺序第一版固定，不做拖拽排序。

---

### 2. 今日

**定位**：今天这一页，当天所有要动的都在这里。左中右三栏。

**左栏 · 今日计划**
- 顶部一个输入框，敲回车即添加一条
- 每条带：标题、优先级（高 / 中 / 低，用颜色点区分）、关联模块（可不选，选了会在对应模块留痕）、完成勾选框
- 可拖动排序；右键或悬浮菜单支持「顺延到明天」「移到学习/开发/咨询」
- 底部显示「已完成 3 / 8」

**中栏 · 快速备忘**
- 便签式多条列表，点开就地编辑，支持置顶和删除
- 不设标题，纯文本，突出「随手记」

**右栏 · 今日打卡 + 今日记录**
- 打卡区：健身（今天练了 / 没练）、饮食（早 / 午 / 晚 / 加餐，逐个勾）
- 记录区：列出今天已产生的学习记录、工时记录、游戏时长记录，可快速补记

**字段（今日计划条目）**：`id`、`标题`、`日期`、`优先级`、`完成状态`、`完成时间`、`关联模块`、`备注`

---

### 3. 学习计划

**定位**：技能学习台账，主线是「方向 → 学习项 → 学习记录」。

三个页签：

**页签 A · 方向总览**
卡片式，每个学习方向一张卡，显示：方向名、目标一句话、目标日期、总进度（按学习项加权算）、累计投入时长、剩余天数。
操作：新增 / 编辑 / 归档方向。

**页签 B · 学习项**
属于某个方向的具体载体（一门课、一本书、一个知识点）。列表可按方向筛选。

**页签 C · 学习记录**
每次学习一条流水：日期、学了哪个学习项、时长（分钟）、笔记。

**字段**

| 实体 | 字段 |
| --- | --- |
| 学习方向 | 名称、目标描述、目标日期、状态（进行中 / 已达成 / 已归档） |
| 学习项 | 名称、所属方向、类型（课程 / 书 / 文档 / 视频 / 其他）、链接、进度%、状态（未开始 / 进行中 / 已完成 / 搁置）、开始日期、预计完成日期 |
| 学习记录 | 日期、学习项、时长（分钟）、笔记内容 |

**联动**：在学习记录里写下笔记后，笔记会按学习项聚合，形成该知识点的笔记沉淀。累计时长同时汇总到方向总览和本周工时感。

---

### 4. 开发工作

**定位**：工程项目台账，主线是「项目 → 任务 → 状态流转」。

**视图 A · 项目列表**
项目卡片：项目名、状态（进行中 / 暂停 / 已完成）、技术栈标签、任务完成比、最近更新时间。

**视图 B · 项目详情（看板）**
三列看板：待办 / 进行中 / 已完成。卡片可拖动改状态。
任务卡片显示：标题、优先级、标签、创建时间。

**视图 C · Bug 记录**
列表：所属项目、Bug 标题、严重级别（高 / 中 / 低）、状态（待修 / 修复中 / 已解决）、复现步骤。支持一键「转成任务」。

**视图 D · 踩坑笔记**
Markdown 笔记，可打标签，支持全文搜索。定位是「以后还会踩的坑」，跟 Bug 区分开。

**字段**

| 实体 | 字段 |
| --- | --- |
| 项目 | 名称、描述、技术栈标签、状态、创建时间、最近更新 |
| 任务 | 标题、描述、所属项目、优先级、状态、标签、创建时间、完成时间 |
| Bug | 标题、所属项目、严重级别、状态、复现步骤、解决方式 |
| 踩坑笔记 | 标题、正文（Markdown）、标签、创建时间 |

---

### 5. 咨询工作

**定位**：客户交付台账，主线是「客户 → 项目 → 交付 + 工时」。

**视图 A · 客户列表**
客户卡片：客户名、联系人、合作状态（洽谈中 / 进行中 / 已结束）、累计工时、累计金额。

**视图 B · 客户详情**
该客户下的项目列表 + 该客户的沟通纪要时间线。

**视图 C · 项目与交付物**
项目：名称、周期（起止）、报价、当前阶段。
交付物清单：名称、状态（待交付 / 已交付）、交付日期、链接。

**视图 D · 沟通纪要**
日期、客户 / 项目、参与人、要点、待跟进事项。待跟进事项可一键「加进今日计划」。

**视图 E · 工时**
记录：日期、所属项目、时长（分钟）、工作内容。
带一个简单计时器：点开始 → 点停止 → 自动生成一条工时记录。
汇总：按客户 / 项目 / 本周 / 本月四种维度看工时，用条形图展示。

**字段**

| 实体 | 字段 |
| --- | --- |
| 客户 | 名称、联系人、联系方式、合作状态、备注 |
| 项目 | 名称、所属客户、开始日期、结束日期、报价、阶段 |
| 交付物 | 名称、所属项目、状态、交付日期、链接 |
| 沟通纪要 | 日期、客户、项目、参与人、要点、待跟进 |
| 工时记录 | 日期、项目、时长（分钟）、工作内容 |

---

### 6. 健身计划

**定位**：轻量打卡，不记重量组数。

**周计划**：周一到周日七行，每行填「练什么」（自由文本，比如"胸肩"、"腿"），可标记某天为休息日。这是一套可复用的模板。

**每日打卡**：今天完成没，点一下打勾，可写一句备注。

**坚持情况**：显示连续打卡天数，下方是本月日历热力图，能一眼看出哪些天练了。

**字段**：周计划行（星期、内容、是否休息日）、打卡记录（日期、是否完成、备注）

---

### 7. 饮食计划

**定位**：轻量打卡，不算热量。

**餐次结构**：每天固定四格 —— 早 / 午 / 晚 / 加餐。

**常吃预设**：可以先把常吃的东西存成常用项，记录时点一下快速填入。

**每日记录**：每格填内容（自由文本），勾选表示按计划吃了。

**往回翻**：按日期倒序看每天吃了什么，支持看某一天。

**字段**：餐次记录（日期、餐次、内容、是否完成、备注）

---

### 8. 游戏娱乐

**定位**：游戏库 + 时长记录。

**视图 A · 游戏库**
卡片 / 表格双模式。每款游戏：名称、平台（PC / 主机 / 手机）、状态（想玩 / 在玩 / 已通关 / 弃坑）、评分（1–10）、短感想。默认按状态分组，「在玩」置顶。

**视图 B · 时长记录**
每条：日期、游戏（下拉选）、时长（分钟）、备注。可快速连加。

**视图 C · 统计**
本周 / 本月各游戏时长排行（条形图）、总时长、连续记录天数。

**字段**

| 实体 | 字段 |
| --- | --- |
| 游戏 | 名称、平台、状态、评分、感想、加入时间、通关时间 |
| 时长记录 | 日期、游戏、时长（分钟）、备注 |

---

### 9. 数据与设置

| 分组 | 内容 |
| --- | --- |
| 数据备份 | 一键导出全部数据为 JSON 文件（浏览器下载）；导入文件，可选「覆盖全部」或「合并」；显示上次备份时间并提醒 |
| 存储占用 | 显示各类数据条数（任务 X 条、笔记 Y 条…）和估算容量 |
| 清空重置 | 危险操作，需要输入确认文字才能执行 |
| 外观 | 主题：跟随时间 / 白天 / 晚上；设置白天时段起止 |
| 偏好 | 一周第一天（周一 / 周日）；打开 App 默认进哪一页 | 
| 关于 | 版本号、数据总量统计 |

---

## 四、数据结构概览

| 实体 | 归属模块 | 关键字段 |
| --- | --- | --- |
| 今日计划 | 今日 | 标题、日期、优先级、完成状态、关联模块 |
| 备忘 | 今日 | 内容、置顶、创建时间 |
| 学习方向 / 学习项 / 学习记录 | 学习计划 | 目标日期、进度%、时长、笔记 |
| 项目 / 任务 / Bug / 踩坑笔记 | 开发工作 | 状态流转、优先级、标签 |
| 客户 / 项目 / 交付物 / 纪要 / 工时 | 咨询工作 | 联系人、报价、周期、时长 |
| 健身周计划 / 健身打卡 | 健身计划 | 星期、内容、是否完成 |
| 餐饮记录 | 饮食计划 | 日期、餐次、内容、是否完成 |
| 游戏 / 游戏时长 | 游戏娱乐 | 状态、评分、时长 |
| 设置 | 数据与设置 | 主题、白天时段、周起始日、默认页 |

各模块数据之间只在少量地方互通（今日计划可关联模块、纪要可转待办），不做深度耦合。

---

## 五、第一版做 / 暂时不做

**第一版要做**

1. 十个页面（首页 + 九个模块）全部可用，导航与主题切换正常
2. 今日计划、快速备忘，并与首页摘要联动
3. 学习计划、开发工作、咨询工作三块的完整记录与状态流转
4. 健身、饮食打卡
5. 游戏库 + 时长记录
6. 本地存储 + 导出 / 导入备份
7. 白天 / 晚上两套主题，全站统一
8. 基础统计：工时汇总、学习时长、游戏时长排行、健身连续天数

**第一版暂时不做**

- 登录、账号、云同步、手机端、部署上线
- 联网抓数据（自动拉平台数据、自动同步）
- 复杂报表、可自定义图表
- 提醒推送、桌面通知、定时提醒
- 多设备 / 多人共享
- 训练重量组数、热量与营养计算
- 密码锁、数据加密
- 卡片拖拽排序、自定义首页布局
- 附件 / 图片上传（游戏封面、交付物截图等）

---

## 六、需要你确认的几个点

1. 上面十个页面的**字段**有没有多余或缺少的？
2. 「工时计时器」和「健身日历热力图」这两个稍复杂的功能，第一版保留吗？
3. 备份只做**手动一键导出**，还是希望每次打开时自动提醒你备份？
