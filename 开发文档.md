# 个人管理 App · 开发文档（可直接执行）

> 版本 v1.0　依据：`PRD.md` v1.0　日期：2026-09-20
> 本文件面向执行者（人或 AI）。严格按第 9 章顺序做，即可产出满足 PRD 全部验收标准的成品。
> 与前序文档冲突时，以 `PRD.md` 为准；实现细节不明时，以本文件为准。

---

## 0. 一句话技术方案

原生 HTML + CSS + JavaScript，**无框架、无构建、无依赖、无网络**。多个文件用普通 `<script>` 标签加载，双击 `index.html` 即可在浏览器里跑。

---

## 1. 技术约束（硬性，不可违反）

| # | 约束 | 原因 |
| --- | --- | --- |
| 1 | 不用任何前端框架（React / Vue / jQuery 等） | 保持零依赖 |
| 2 | 不用任何 CDN、外链字体、外链图标库 | 断网必须可用 |
| 3 | **禁止 `type="module"`** | `file://` 协议下 ES Module 会被 CORS 拦截，双击打不开 |
| 4 | 不用构建工具，没有 package.json / vite / webpack | 改完直接刷新即可 |
| 5 | 图标用内联 SVG 或 Unicode 字符，不引图标字体 | 同上 |
| 6 | 语法基线 ES2018（可用 `async/await`、可选链、模板字符串、展开运算符） | 现代浏览器 |
| 7 | 全部文件 UTF-8 编码，HTML 声明 `<meta charset="utf-8">` | 中文不乱码 |
| 8 | 目标浏览器：Chrome / Edge 最新版 | 只考虑自己这台电脑 |

---

## 2. 目录结构

```
D:\我的app\
├── index.html                    单页外壳，唯一入口
├── style.css                     全部样式（含两套主题变量）
├── js/
│   ├── core/
│   │   ├── util.js               工具函数（日期、格式化、id、DOM 助手）
│   │   ├── schema.js             数据结构定义、默认值、存储 key 清单
│   │   ├── store.js              存储层（IndexedDB 优先 / localStorage 降级）
│   │   ├── ui.js                 15 个通用组件
│   │   └── router.js             哈希路由
│   ├── modules/
│   │   ├── home.js               首页总览
│   │   ├── today.js              今日
│   │   ├── study.js              学习计划
│   │   ├── dev.js                开发工作
│   │   ├── consult.js            咨询工作
│   │   ├── fitness.js            健身计划
│   │   ├── diet.js               饮食计划
│   │   ├── game.js               游戏娱乐
│   │   └── settings.js           数据与设置
│   └── app.js                    启动、注册页面、主题、全局快捷键
└── PRD.md / 开发文档.md           文档
```

### 2.1 index.html 脚本加载顺序（有依赖，不可颠倒）

```html
<script src="js/core/util.js"></script>
<script src="js/core/schema.js"></script>
<script src="js/core/store.js"></script>
<script src="js/core/ui.js"></script>
<script src="js/core/router.js"></script>
<script src="js/modules/home.js"></script>
<script src="js/modules/today.js"></script>
<script src="js/modules/study.js"></script>
<script src="js/modules/dev.js"></script>
<script src="js/modules/consult.js"></script>
<script src="js/modules/fitness.js"></script>
<script src="js/modules/diet.js"></script>
<script src="js/modules/game.js"></script>
<script src="js/modules/settings.js"></script>
<script src="js/app.js"></script>
```

全局命名空间统一挂在 `window.App` 下：`App.util`、`App.schema`、`App.store`、`App.ui`、`App.router`、`App.pages`。

### 2.2 index.html 骨架

```html
<!DOCTYPE html>
<html lang="zh-CN" data-theme="light">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>日新 · Rixin</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <aside id="sidebar"></aside>
  <main id="main">
    <header id="topbar"></header>
    <section id="page-host"></section>
  </main>
  <div id="overlay-host"></div>
  <div id="toast-host"></div>
  <!-- 上面的 script 列表 -->
</body>
</html>
```

---

## 3. 数据结构（完整定义）

### 3.1 存储 key 清单

统一前缀 `pm.`。每个集合一个 key，value 为数组或对象。

| key | 值类型 | 归属模块 |
| --- | --- | --- |
| `pm.meta` | Object | 系统 |
| `pm.settings` | Object | 数据与设置 |
| `pm.tasks` | Array | 今日 |
| `pm.notes` | Array | 今日 |
| `pm.study.directions` | Array | 学习计划 |
| `pm.study.items` | Array | 学习计划 |
| `pm.study.logs` | Array | 学习计划 |
| `pm.dev.projects` | Array | 开发工作 |
| `pm.dev.tasks` | Array | 开发工作 |
| `pm.dev.bugs` | Array | 开发工作 |
| `pm.dev.notes` | Array | 开发工作 |
| `pm.consult.clients` | Array | 咨询工作 |
| `pm.consult.projects` | Array | 咨询工作 |
| `pm.consult.deliverables` | Array | 咨询工作 |
| `pm.consult.meetings` | Array | 咨询工作 |
| `pm.consult.worklogs` | Array | 咨询工作 |
| `pm.fitness.plan` | Array | 健身计划 |
| `pm.fitness.logs` | Array | 健身计划 |
| `pm.diet.records` | Array | 饮食计划 |
| `pm.diet.favorites` | Array | 饮食计划 |
| `pm.game.games` | Array | 游戏娱乐 |
| `pm.game.logs` | Array | 游戏娱乐 |

### 3.2 实体字段表

**pm.meta**
```
{ schemaVersion: 1, createdAt: Number, lastBackupAt: Number|null, lastExportAt: Number|null }
```

**pm.settings**
```
{ theme: 'auto'|'light'|'dark'        // 默认 'auto'
  dayStart: '06:00', dayEnd: '18:00'  // 默认白天时段
  weekStartsOn: 1|0                   // 1=周一(默认) 0=周日
  defaultPage: 'home'                 // 打开默认进哪页
  backupRemindDays: 7                 // 超过多少天提醒备份
}
```

**pm.tasks（今日计划）**
```
{ id, title:String, date:'YYYY-MM-DD', priority:'high'|'mid'|'low',
  done:Boolean, doneAt:Number|null,
  moduleRef:{ module:String, id:String }|null,   // 关联模块
  note:String, order:Number, createdAt:Number }
```

**pm.notes（快速备忘）**
```
{ id, content:String, pinned:Boolean, createdAt:Number, updatedAt:Number }
```

**pm.study.directions**
```
{ id, name, goal, targetDate:'YYYY-MM-DD'|'', status:'active'|'done'|'archived', createdAt }
```

**pm.study.items**
```
{ id, directionId, name, type:'course'|'book'|'doc'|'video'|'other',
  url, progress:Number(0-100), status:'todo'|'doing'|'done'|'paused',
  startDate, dueDate, createdAt }
```

**pm.study.logs**
```
{ id, date, itemId, minutes:Number, note:String, createdAt }
```

**pm.dev.projects**
```
{ id, name, desc, stack:[String], status:'doing'|'paused'|'done',
  createdAt, updatedAt }
```

**pm.dev.tasks**
```
{ id, projectId, title, desc, priority:'high'|'mid'|'low',
  status:'todo'|'doing'|'done', tags:[String], createdAt, doneAt:Number|null }
```

**pm.dev.bugs**
```
{ id, projectId, title, level:'high'|'mid'|'low',
  status:'open'|'fixing'|'resolved', steps, solution, createdAt }
```

**pm.dev.notes（踩坑笔记）**
```
{ id, title, body:String(Markdown 原文), tags:[String], createdAt, updatedAt }
```

**pm.consult.clients**
```
{ id, name, contact, phone, status:'talking'|'doing'|'closed', note, createdAt }
```

**pm.consult.projects**
```
{ id, clientId, name, startDate, endDate, quote:Number, stage:String, createdAt }
```

**pm.consult.deliverables**
```
{ id, projectId, name, status:'pending'|'delivered', deliveredAt, url }
```

**pm.consult.meetings（沟通纪要）**
```
{ id, date, clientId, projectId, attendees, points, followUp, followUpDone:Boolean, createdAt }
```

**pm.consult.worklogs（工时）**
```
{ id, date, projectId, minutes:Number, content, createdAt }
```

**pm.fitness.plan（周计划，固定 7 条）**
```
{ id, weekday:1..7, content, rest:Boolean }
```

**pm.fitness.logs**
```
{ id, date, done:Boolean, note }
```

**pm.diet.records**
```
{ id, date, meal:'breakfast'|'lunch'|'dinner'|'snack',
  content, done:Boolean, note }
```

**pm.diet.favorites**
```
{ id, content, order:Number }
```

**pm.game.games**
```
{ id, name, platform:'pc'|'console'|'mobile', status:'wish'|'playing'|'cleared'|'dropped',
  rating:Number(1-10)|0, thought, addedAt, clearedAt:Number|null }
```

**pm.game.logs**
```
{ id, date, gameId, minutes:Number, note }
```

### 3.3 id、排序、日期约定

- **id 生成**：`App.util.uid(prefix)` → `前缀_时间戳36进制_随机4位`，例：`task_m1x2y3_9f3a`
- **排序**：数组顺序即显示顺序。需要显式排序的（今日计划）用 `order` 整数，新增时取 `max(order)+1`
- **日期**：统一存 `'YYYY-MM-DD'` 本地日期字符串，**禁止用 `toISOString()`**（会因时区偏移一天）。用 `App.util.today()` 和 `App.util.fmtDate(d)`
- **时间戳**：`Date.now()`，数字类型
- **时长**：统一存**分钟**（整数）。显示时用 `App.util.fmtDuration(minutes)` → `1h30m` / `45m`

---

## 4. 存储层 API（`js/core/store.js`）

### 4.1 设计

对外是一个同步风格的接口，内部先用 IndexedDB，失败自动降级 localStorage。

```js
App.store = {
  async init(),                  // 必须在 App.start() 之前 await
  get(key, fallback),            // 读，读不到返回 fallback
  set(key, value),               // 写（异步落盘，同步返回）
  all(),                         // 返回全部 pm.* 数据的对象
  snapshot(key),                 // 变更前快照，供撤销用
  undo(),                        // 撤销上一次 snapshot
  exportJSON(),                  // 返回 { schemaVersion, exportedAt, app, data }
  importPreview(payload),        // 返回 { total, add, overwrite, byKey:[] }
  importApply(payload, mode),    // mode: 'overwrite'|'merge'
  clearAll(),                    // 清空全部 pm.* 数据
  usage()                        // 返回 { count, bytes } 估算占用
}
```

### 4.2 双后端实现要点

```js
// 伪代码骨架
const DB_NAME = 'personal-manager', STORE = 'kv';
let backend = null;

async function tryIDB() {
  return new Promise(resolve => {
    let req;
    try { req = indexedDB.open(DB_NAME, 1); }   // file:// 下可能直接抛异常
    catch (e) { return resolve(null); }
    req.onupgradeneeded = () => req.result.createObjectStore(STORE);
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => resolve(null);
    req.onblocked = () => resolve(null);
    setTimeout(() => resolve(null), 1500);      // 超时兜底
  });
}

App.store.init = async function () {
  const db = await tryIDB();
  if (db) {
    backend = { kind: 'idb', db };
    // 写一条探针验证可写；失败则 backend = null
  }
  if (!backend) {
    backend = { kind: 'ls' };                   // localStorage 降级
  }
  this._cache = this._loadAllCache();           // 启动时全量读进内存
};
```

**关键点**

1. 内存缓存 `_cache` 是唯一读取源，`get()` 直接读缓存，不走异步；`set()` 同时更新缓存并异步落盘
2. localStorage 后端把所有 `pm.*` key 平铺存放；IndexedDB 后端放在一个 object store 里
3. `App.store.ready` 为 `true` 才算初始化完成，`App.start()` 里 `await App.store.init()`
4. **页面渲染一律用缓存**，不写 `await`，保证渲染同步、无闪烁

### 4.3 导出文件格式

```json
{
  "app": "personal-manager",
  "schemaVersion": 1,
  "exportedAt": 1789000000000,
  "data": { "pm.tasks": [], "pm.notes": [], "...": "..." }
}
```

文件名：`个人管理-备份-YYYYMMDD.json`（用 `App.util.fmtDate(new Date(), '')` 生成）

### 4.4 导入预览与合并规则

- **预览**：逐 key 比对，集合类按 `id` 求交集 → 交集条数记为 `overwrite`，文件里有而本地没有的记为 `add`；对象类（settings/meta）整体记为 `overwrite` 1 条
- **覆盖**：清空全部 `pm.*`，写入文件里的数据
- **合并**：按 id 去重，**同 id 以文件里的为准**，其余追加

### 4.5 撤销

```js
// 用法：任何删除/危险修改前先快照
App.store.snapshot('pm.tasks');
App.store.set('pm.tasks', newArr);
App.ui.toast('已删除 1 条', { undo: true });   // toast 的撤销按钮调 App.store.undo()
```
撤销栈只保留最近 **20** 次快照，5 秒后 toast 消失即不可撤销。

---

## 5. 路由与页面契约

### 5.1 路由表（实现时必须与此一致）

```js
const ROUTES = [
  { id: 'home',    hash: '#/home',                title: '首页总览',  sub: '今天该关注什么' },
  { id: 'today',   hash: '#/today',               title: '今日',      sub: '今天的计划、备忘和打卡' },
  { id: 'study',   hash: '#/study',               title: '学习计划',  sub: '方向、学习项和学习记录' },
  { id: 'dev',     hash: '#/dev',                 title: '开发工作',  sub: '项目、Bug 与踩坑笔记' },
  { id: 'devProject', hash: '#/dev/project/:id',  title: '项目看板',  sub: '三列拖拽改状态' },
  { id: 'consult', hash: '#/consult',             title: '咨询工作',  sub: '客户、交付与工时' },
  { id: 'consultClient', hash: '#/consult/client/:id', title: '客户详情', sub: '项目、交付物与纪要' },
  { id: 'fitness', hash: '#/fitness',             title: '健身计划',  sub: '每周安排与打卡' },
  { id: 'diet',    hash: '#/diet',                title: '饮食计划',  sub: '三餐记录与打卡' },
  { id: 'game',    hash: '#/game',                title: '游戏娱乐',  sub: '游戏库与时长' },
  { id: 'settings',hash: '#/settings',            title: '数据与设置', sub: '备份、外观与偏好' }
];
```

### 5.2 页面对象契约

每个模块文件向 `App.pages` 注册一个对象：

```js
App.pages.dev = {
  id: 'dev',
  title: '开发工作',
  subtitle: '项目、Bug 与踩坑笔记',

  // 路由参数（二级路由才有）
  param: null,

  // 渲染：把内容写进 #page-host，必须同步完成
  render(root, param) { /* ... */ },

  // 数据变化后重绘自己（默认调用 render，特殊页面可覆写做局部更新）
  refresh() { this.render(document.getElementById('page-host'), this.param); },

  // 离开页面时清理（计时器、定时器等）
  destroy() {}
};
```

### 5.3 渲染与刷新规则

| 场景 | 做法 |
| --- | --- |
| 切页面 | `Router` 调用 `curr.destroy()` → `next.render(host, param)` |
| 数据变更 | 调用 `App.pages[current].refresh()` |
| **输入中的控件**（新增框、搜索框、筛选框） | **不能**跟着全量重绘，否则会丢焦点。做法：这些控件独立包在 `<div id="xxx-input">` 里，刷新时只重绘结果区 `<div id="xxx-list">` |
| 需要保留焦点时 | 重绘前记录 `document.activeElement.dataset.fk`，重绘后 `querySelector('[data-fk="..."]').focus()` |

### 5.4 事件绑定方式

统一用**事件委托**：`render()` 里给容器一次性绑一个 `click` / `change` / `keydown` 监听，通过 `e.target.closest('[data-act]')` 取动作名和 `data-id`。

```js
root.querySelector('#task-list').onclick = e => {
  const el = e.target.closest('[data-act]');
  if (!el) return;
  const { act, id } = el.dataset;
  if (act === 'toggle') TodoActions.toggle(id);
  if (act === 'del')    TodoActions.remove(id);
};
```

`render()` 会多次执行，**每次重绘前清空容器的 `onclick` 赋值即可避免重复绑定**（用 `onclick =` 赋值而不是 `addEventListener`）。

---

## 6. 通用组件 API（`js/core/ui.js`）

所有组件都是**纯函数，返回 HTML 字符串**，不直接操作 DOM。需要交互的在返回的 HTML 上打 `data-act`，由页面委托处理。

| 组件 | 签名 | 说明 |
| --- | --- | --- |
| `ui.card` | `(title, bodyHTML, opts)` | 卡片容器，`opts.clickable` 加 `data-act="open"` |
| `ui.row` | `(leftHTML, rightHTML, opts)` | 列表行，`opts.actions` 右侧操作区 |
| `ui.board` | `(columns, cardsHTML)` | 看板容器，三列，列头带计数 |
| `ui.tag` | `(text, tone)` | 标签，`tone`: `''|'info'|'ok'|'warn'|'danger'` |
| `ui.progress` | `(percent, opts)` | 进度条，`opts.showText` 显示百分比 |
| `ui.check` | `(checked, opts)` | 勾选框，`opts.act` 指定动作名 |
| `ui.bar` | `(items, opts)` | 横向条形图，`items:[{label, value, max}]` |
| `ui.heatmap` | `(year, month, dayMap, opts)` | 月历热力图，`dayMap` 为 `{'YYYY-MM-DD': level}` |
| `ui.drawer` | `(title, bodyHTML, opts)` | 右侧抽屉，`ui.closeDrawer()` 关闭 |
| `ui.confirm` | `(msg, onOk, opts)` | 二次确认框，返回 Promise |
| `ui.toast` | `(msg, opts)` | 右下角提示，`opts.undo` 显示撤销按钮 |
| `ui.empty` | `(text, actionHTML)` | 空状态 |
| `ui.filterBar` | `(fields)` | 筛选条，`fields:[{key,label,type,options}]` |
| `ui.dateNav` | `(date, opts)` | 日期切换 `← 9月20日 周日 →` |
| `ui.tabs` | `(tabs, activeId, opts)` | 页签，`tabs:[{id,label}]` |

组件内部一律使用 CSS 变量，不写死颜色。

---

## 7. 主题与样式规范

### 7.1 主题切换机制

```js
// app.js
function applyTheme() {
  const s = App.store.get('pm.settings', App.schema.defaultSettings);
  let t = s.theme;
  if (t === 'auto') {
    const now = App.util.hhmm();                       // 例 '14:30'
    t = (now >= s.dayStart && now < s.dayEnd) ? 'light' : 'dark';
  }
  document.documentElement.dataset.theme = t;
}
setInterval(applyTheme, 60 * 1000);   // 每分钟检查一次，跨过白天时段自动切
```

### 7.2 CSS 变量两套值（必须照此实现，保证对比度）

```css
:root, [data-theme="light"] {
  --bg:            #F7F7F5;
  --panel:         #FFFFFF;
  --panel-2:       #F1EFE8;
  --border:        #E3E1DA;
  --border-strong: #C9C7BF;
  --text:          #2C2C2A;
  --text-2:        #5F5E5A;
  --text-3:        #8A8880;
  --accent:        #185FA5;
  --accent-weak:   #E6F1FB;
  --accent-text:   #0C447C;
  --ok:            #3B6D11;  --ok-weak:     #EAF3DE;
  --warn:          #854F0B;  --warn-weak:   #FAEEDA;
  --danger:        #A32D2D;  --danger-weak: #FCEBEB;
  --bar:           #85B7EB;
  --heat-0:        #EDEBE4;  --heat-1: #C0DD97;  --heat-2: #639922;  --heat-rest: #D3D1C7;
}
[data-theme="dark"] {
  --bg:            #1C1C1A;
  --panel:         #262624;
  --panel-2:       #2F2F2C;
  --border:        #3A3A36;
  --border-strong: #4E4E49;
  --text:          #ECEAE4;
  --text-2:        #B4B2A9;
  --text-3:        #8A8880;
  --accent:        #85B7EB;
  --accent-weak:   #1E3A55;
  --accent-text:   #B5D4F4;
  --ok:            #97C459;  --ok-weak:     #26331A;
  --warn:          #EF9F27;  --warn-weak:   #3A2C13;
  --danger:        #F09595;  --danger-weak: #3D1F1F;
  --bar:           #378ADD;
  --heat-0:        #2F2F2C;  --heat-1: #3B6D11;  --heat-2: #97C459;  --heat-rest: #444441;
}
```

**规矩**：任何 CSS 里都不许出现裸色值，只能引用变量。这样切主题一定是全站生效。

### 7.3 尺寸规范

| 项 | 值 |
| --- | --- |
| 侧边栏宽 | 200px（收起 56px） |
| 顶栏高 | 56px |
| 圆角 | 卡片 8px / 小控件 6px / 大容器 12px |
| 字号 | 页面标题 20px·500 / 卡片标题 14px·500 / 正文 13px·400 / 次要 12px·400 |
| 字重 | 只用 400 和 500 |
| 间距基数 | 4px，常用 8 / 12 / 16 / 24 |
| 断点 | ≥1440 三栏 / 1024–1440 两栏 / <1024 单栏+侧边栏图标化 |

---

## 8. 各模块实现要点

### 8.1 今日（`today.js`）

- 三栏布局：左 5 / 中 3.5 / 右 3，用 CSS Grid `grid-template-columns: 5fr 3.5fr 3fr`
- 左栏：`#task-input` 独立容器（不被列表重绘影响），`#task-list` 为列表容器
- 输入框 `keydown` 监听 Enter → 读 `#task-input` 的 value、优先级下拉、关联模块下拉 → 建条目 → **只重绘 `#task-list`** → 清空输入框并保持焦点
- 勾选完成：改 `done`，`doneAt = done ? Date.now() : null`，标题加 `.done` 类（变浅 + 删除线）
- 拖拽排序：用 HTML5 `draggable` + `dragstart/dragover/drop`，落点后重排 `order` 并写库
- 「顺延到明天」：`date` 改为明天，`order` 取明天最大值 +1
- 右栏打卡：健身按钮读 `pm.fitness.logs` 里今天的记录，有则填充色、无则空心
- 连续天数计算 `calcStreak()`：从今天往前逐日，命中「计划内且完成」则 +1，遇休息日跳过不中断，遇计划内未完成则停止
- 今日记录区：聚合 `pm.study.logs`、`pm.consult.worklogs`、`pm.game.logs` 中 `date === 今天` 的条目

### 8.2 学习计划（`study.js`）

- 三个页签用 `ui.tabs`，页签状态存在模块内部变量（不落库）
- 方向卡片进度 = 该方向下所有学习项 `progress` 的平均值（无学习项时为 0）
- 累计时长 = 汇总 `pm.study.logs` 中 `itemId` 属于该方向下学习项的 `minutes`
- 时长输入解析 `parseDuration(str)`：支持 `90` / `1.5h` / `1h30m`，返回分钟数；解析失败给 Toast 提示
- 记一笔后弹出确认条「进度要 +2% 吗？」，确认才改 `progress`，**默认不改**
- 学习项详情里的笔记列表 = `pm.study.logs` 按 `itemId` 过滤且 `note` 非空

### 8.3 开发工作（`dev.js`）

- 项目看板：三列由 `ui.board` 渲染；拖拽复用今日计划的拖拽工具函数，落到不同列即改 `status`；落到 `done` 时 `doneAt = Date.now()`
- 看板列头计数从数组实时算
- Bug「转成任务」：在 `pm.dev.tasks` 追加一条 `{ projectId, title: bug.title, priority: bug.level 映射, status:'todo' }`，并把 `bug.status = 'resolved'`，一次写入两个 key
- 踩坑笔记：左侧列表 + 右侧正文。正文渲染用**极简 Markdown**：只支持 `# ## ###`、`- `、`**粗体**`、`` `代码` ``、换行，自己写 20 行正则处理，不引库
- 全文搜索：对 `title + body` 做 `includes` 匹配（忽略大小写），不引模糊匹配

### 8.4 咨询工作（`consult.js`）

- 客户卡片上「累计工时」= 汇总该客户所有项目下的 worklog 分钟数；「累计金额」= 各项目 quote 之和（第一版直接从项目报价取，不做按工时结算）
- 计时器：`setInterval` 每秒刷新显示；开始时间存模块内部变量**同时写进 `sessionStorage`**（防刷新丢失）；停止时按 `结束时间 - 开始时间` 生成 worklog
- 超过 4 小时（14400 秒）弹一次 `ui.confirm('还在计时，要停吗？')`
- 离开页面时 `destroy()` 里 `clearInterval`，计时本身不受影响（靠 `sessionStorage` 恢复）
- 工时汇总：四组切换（客户 / 项目 / 本周 / 本月），每组算 `items:[{label, value}]` 后交给 `ui.bar`
- 纪要「待跟进」勾选 → 在 `pm.tasks` 新增 `{ title: followUp, date: 今天, moduleRef:{module:'consult', id: meeting.id} }`

### 8.5 健身计划（`fitness.js`）

- 周计划固定 7 条，首次进入自动初始化 `weekday 1..7`，`content=''`，`rest=false`
- 热力图数据：遍历当月每一天，今天之前的日期在 `pm.fitness.logs` 里有 `done=true` → level 2；有计划但没完成 → level 1；休息日 → `rest` 色；未来日期 → level 0
- 打卡按钮切换今天的 `done`，没有当天记录就新建
- 连续天数复用今日页的 `calcStreak()`（提到 `util.js` 里共用）

### 8.6 饮食计划（`diet.js`）

- 顶部 `ui.dateNav` 控制当前查看日期，默认今天
- 四格固定顺序：breakfast / lunch / dinner / snack
- 内容输入框失焦保存；有内容自动 `done = true`，清空内容自动 `done = false`
- 常用项：`ui.tag` 形式点击填入当前聚焦的格子
- 历史：按日期倒序分组的折叠列表，默认全部收起

### 8.7 游戏娱乐（`game.js`）

- 游戏库按 `status` 分组渲染，`playing` 组排最前
- 卡片 / 表格模式切换存在模块内部变量
- 快速记录条：游戏下拉（按 `status` 排序，`playing` 在前）+ 分钟数 + Enter 提交；输入 `+30` 时给最后一条记录加 30 分钟
- 游戏状态改为 `cleared` 时若 `clearedAt` 为空则设为 `Date.now()`
- 统计：本周 / 本月切换，按游戏汇总分钟数，交给 `ui.bar`

### 8.8 数据与设置（`settings.js`）

- 导出：`App.store.exportJSON()` → `JSON.stringify` → `Blob` → `URL.createObjectURL` → 触发 `<a download>` 点击 → 写 `lastExportAt` / `lastBackupAt`
- 导入流程：选文件 → `FileReader` 读 → `JSON.parse` → 校验 `app === 'personal-manager'` → `ui.drawer` 里展示 `importPreview` 结果 → **弹出选择框让用户选覆盖 / 合并**（每次必弹）→ `ui.confirm` 二次确认 → `importApply` → Toast → 重绘
- 清空：先自动执行一次导出（快照），再要求输入「确认清空」四字，才允许点确认
- 外观：主题三选一（radio）+ 白天时段两个 `<input type="time">`
- 存储占用：`App.store.usage()`，逐 key 显示条数
- 关于：版本号 + 各模块数据量

### 8.9 首页总览（`home.js`）

- 七张卡片，用 CSS Grid，三列自适应（`repeat(auto-fit, minmax(280px, 1fr))`）
- 除「今日计划」卡片的勾选和「快速备忘」卡片的输入外，其余全部只读
- 卡片点击：整卡 `data-act="open"` + `data-module`，委托里调 `App.router.go(module)`
- 备份提醒条：比较 `Date.now() - lastBackupAt > backupRemindDays * 86400000` 才显示；用户关闭后把当天日期写进 `pm.meta.backupTipClosedAt`，当天不再显示

### 8.10 app.js

```js
App.start = async function () {
  await App.store.init();
  App.router.init();
  App.sidebar.render();
  App.topbar.render();
  bindGlobalKeys();          // / N Ctrl+1~9 Esc
  applyTheme();
  setInterval(applyTheme, 60000);
  App.router.go(App.store.get('pm.settings').defaultPage || 'home');
};
window.addEventListener('DOMContentLoaded', App.start);
```

全局快捷键：`/` 聚焦顶栏搜索、`N` 触发当前页主新增动作（页面对象上实现 `primaryAdd()`，未实现则忽略）、`Ctrl+1~9` 切到导航前九项、`Esc` 关闭所有浮层。

---

## 9. 执行任务清单

按顺序做，每一步做完先自测再进下一步。每步都给出**产出**和**自测**。

### Step 1 · 外壳骨架
**做**：`index.html` + `style.css`（含两套主题变量）+ `app.js` + `router.js`。侧边栏 10 个入口、顶栏、内容区占位。
**产出**：双击能打开，侧边栏能点，点完地址栏 hash 变化，内容区显示页面标题。
**自测**：对应 PRD 验收 **A1 / A3**。

### Step 2 · 存储层
**做**：`util.js` + `schema.js` + `store.js`。实现 `init/get/set/exportJSON/importPreview/importApply/clearAll/usage`。
**产出**：控制台执行 `App.store.set('pm.tasks', [])` 后刷新，`App.store.get('pm.tasks')` 能读到。
**自测**：对应 **C1**（先用手工数据验）。

### Step 3 · 通用组件
**做**：`ui.js` 全部 15 个组件 + Toast / Drawer / Confirm 的浮层宿主。
**产出**：临时在首页堆一屏组件预览，检查两套主题下的样式。
**自测**：对应 **B1**。

### Step 4 · 主题与响应式
**做**：主题自动切换、手动锁定、三档断点、侧边栏收起。
**产出**：切主题全站变色；窗口缩到 900px 布局重排。
**自测**：对应 **B1 / B2 / B3 / A4**。

### Step 5 · 今日
**做**：三栏、计划增删改拖、备忘、打卡、今日记录、日期切换。
**产出**：今日页全功能可用。
**自测**：对应 **D1 / D7 / D8 的前半 / D11**。

### Step 6 · 学习计划
**做**：三个页签、方向卡片、学习项表格、学习记录、笔记聚合、时长解析。
**自测**：对应 **D2**。

### Step 7 · 开发工作
**做**：项目列表、看板拖拽、任务增删改、Bug 与转任务、踩坑笔记与搜索。
**自测**：对应 **D3 / D4**。

### Step 8 · 咨询工作
**做**：客户列表与详情、纪要、交付物、工时记录、计时器、四组汇总。
**自测**：对应 **D5 / D6**。

### Step 9 · 健身计划
**做**：周计划、打卡、连续天数、月历热力图、补记。
**自测**：对应 **D7**。

### Step 10 · 饮食计划
**做**：四餐记录、常用项、日期切换、历史。
**自测**：对应 **D8**。

### Step 11 · 游戏娱乐
**做**：游戏库（卡片/表格）、状态分组、评分、时长快速记录、统计排行。
**自测**：对应 **D9**。

### Step 12 · 数据与设置
**做**：导出、导入（预览 + 覆盖/合并选择）、清空兜底、存储占用、外观、偏好、关于。
**自测**：对应 **E1–E7**。

### Step 13 · 首页总览
**做**：七张摘要卡、跳转、就地勾选与便签、备份提醒条。
**自测**：对应 **D11 / E6**。

### Step 14 · 全局收尾
**做**：空状态补全、筛选条、快捷键、删除撤销、性能检查。
**自测**：对应 **C1–C4 / D10 / F1–F4 / G1**。

---

## 10. 验收对照总表

| PRD 验收项 | 对应任务步骤 | 具体怎么验 |
| --- | --- | --- |
| A1 双击即用 | Step 1 | 双击打开，F12 控制台无红色报错 |
| A2 无需联网 | Step 1 起 | 断网操作，Network 面板零请求 |
| A3 路由切换 | Step 1 | 点 10 个入口，页面不整刷 |
| A4 响应式 | Step 4 | 缩到 900px，侧边栏图标化、单栏、无横向滚动条 |
| B1 晚上主题 | Step 3/4 | 切晚上，10 个页面无残留亮块 |
| B2 主题记忆 | Step 4 | 锁定晚上后刷新，仍是晚上 |
| B3 自动切换 | Step 4 | 改白天时段使当前时间落在区间外，重开变深色 |
| C1 刷新不丢 | Step 2 | 建 5 任务 3 便签后 F5 |
| C2 关页面不丢 | Step 2 | 关标签重开文件 |
| C3 重启不丢 | Step 2 | 重启电脑后打开 |
| C4 各模块落库 | Step 5–12 | 九个模块各新增 1 条后刷新 |
| D1 今日计划 | Step 5 | 新建/勾选/拖拽/顺延四项逐一验 |
| D2 学习计划 | Step 6 | 进度联动 + 90 分钟入库 |
| D3 开发看板 | Step 7 | 跨列拖动 + 自动盖完成时间 |
| D4 Bug 转任务 | Step 7 | 待办列多卡 + Bug 变已解决 |
| D5 咨询计时 | Step 8 | 跑 60 秒停，生成约 1 分钟记录 |
| D6 工时汇总 | Step 8 | 四组切换数值与条长一致 |
| D7 健身打卡 | Step 9 | 连续天数 0→1，再点回落 |
| D8 饮食打卡 | Step 10 | 四格自动勾选 + 可看昨天 |
| D9 游戏时长 | Step 11 | 30 分钟后排行第一 |
| D10 空状态 | Step 14 | 十个页面逐个看 |
| D11 首页联动 | Step 13 | 改数据回首页看卡片变化 |
| E1–E7 备份恢复 | Step 12 | 导出→清浏览器数据→导入→核对条数 |
| F1–F4 交互容错 | Step 14 | 撤销、就地编辑、快捷键、筛选 |
| G1 性能 | Step 14 | 造 5000 条数据后再测 |

---

## 11. 已知限制与风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| `file://` 下 IndexedDB 可能被浏览器禁用 | 主存储不可用 | 已设计 localStorage 自动降级；两条路径都要测 |
| localStorage 容量约 5MB 且与同源 file 页面共享 | 数据量大时写不进去 | 设置页显示占用；写入失败时 Toast 明确提示「请导出备份并清理」 |
| 清除浏览器数据 / 换浏览器 / 无痕模式 | 数据丢失 | 靠导出的 JSON 文件恢复；7 天未备份给提示条 |
| 无构建、无框架，代码量大后维护变难 | 后期改动成本 | 严格按第 2 章目录分文件，每个模块一个文件，互不引用 |
| 页面全量重绘导致输入框失焦 | 录入体验 | 输入中的控件必须放在独立容器，只重绘结果区（见 5.3） |
| 拖拽在缩放的浏览器下有偏移 | 排序不准 | 拖拽只在列表内部生效，用 `dragover` 计算插入位置，不用坐标换算 |

---

## 12. 开发约定速查

- 命名：文件名小写；函数与变量 `camelCase`；常量 `UPPER_SNAKE`；CSS 类名 `kebab-case`
- 一个模块文件只导出一个 `App.pages.xxx` 对象，模块内部辅助函数全部包在 IIFE 里，不污染全局
- 所有写库操作后面跟一次 `refresh()`，不要手动改 DOM
- 日期一律走 `App.util.today()` / `App.util.fmtDate()`，禁止直接用 `toISOString()`
- 时长一律存分钟，显示走 `App.util.fmtDuration()`
- 颜色一律用 CSS 变量，禁止裸色值
- 每完成一步，按第 9 章的自测项验一遍再往下走，不要攒到最后一起测
